from __future__ import annotations

from fastapi import APIRouter
from fastapi.responses import HTMLResponse, RedirectResponse

router = APIRouter()

_PAGE_TITLES = {
    "demo": "上手演示",
    "developer": "开发者：Credits 充值",
    "finance": "财务：交易与退款",
    "fde": "FDE：问题排查",
    "vcc": "企业用卡申请",
}

_HTML = r'''<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{{TITLE}} · DemoAI</title>
  <style>
    :root{--bg:#f6f8fc;--ink:#172033;--muted:#68738a;--card:#fff;--line:#e3e8f0;--brand:#405cf5;--brand-soft:#eef1ff;--good:#16875d;--good-soft:#eaf8f1;--warn:#a96500;--warn-soft:#fff7e7;--bad:#c43d56;--bad-soft:#fff0f2;--nav:#121a2d;--nav-soft:#27345a}
    *{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:14px/1.55 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.shell{display:flex;min-height:100vh}.side{width:236px;background:var(--nav);color:#dfe6ff;padding:24px 15px;flex:0 0 236px}.brand{font-size:20px;font-weight:760;padding:0 12px 3px}.sub{color:#91a0c6;font-size:12px;padding:0 12px 22px}.nav{display:block;color:#b6c2e2;text-decoration:none;padding:10px 12px;border-radius:9px;margin:3px 0}.nav:hover,.nav.active{background:var(--nav-soft);color:#fff}.side-note{border-top:1px solid #2c385a;margin-top:24px;padding-top:17px;line-height:1.7}.main{flex:1;max-width:1280px;padding:28px 38px;min-width:0}.top{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:20px}.eyebrow{font-size:12px;color:var(--muted);margin-bottom:3px}.title{font-size:27px;font-weight:760;letter-spacing:-.02em}.env{display:flex;gap:7px;align-items:center;justify-content:flex-end;flex-wrap:wrap;max-width:660px}.chip{display:inline-flex;align-items:center;gap:5px;padding:5px 9px;border-radius:999px;font-size:12px;font-weight:650;background:#edf1f8;color:#4b5872;white-space:nowrap}.chip.good{background:var(--good-soft);color:var(--good)}.chip.warn{background:var(--warn-soft);color:var(--warn)}.notice{padding:11px 13px;background:#fff8e7;border:1px solid #f1d59b;border-radius:9px;color:#80520d;margin-bottom:17px}.grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:16px}.card{grid-column:span 12;background:var(--card);border:1px solid var(--line);border-radius:14px;padding:20px;box-shadow:0 2px 8px #20305a08;min-width:0}.half{grid-column:span 6}.third{grid-column:span 4}.metric{font-size:30px;font-weight:760;margin:5px 0}.muted{color:var(--muted)}.small{font-size:12px}h2,h3,h4{margin:0 0 9px}h2{font-size:24px}h3{font-size:17px}h4{font-size:14px}p{margin:7px 0 12px}label{display:block;font-size:12px;color:var(--muted);margin:10px 0 5px}input,textarea,select{width:100%;padding:10px 11px;border:1px solid #ccd4e2;border-radius:8px;background:#fff;color:var(--ink);font:inherit}input:focus,textarea:focus{outline:3px solid #dfe4ff;border-color:var(--brand)}textarea{min-height:105px;resize:vertical}button{border:0;border-radius:8px;padding:10px 15px;background:var(--brand);color:#fff;font-weight:680;cursor:pointer;margin:12px 5px 0 0}button.secondary{background:#e9edf6;color:#283652}button.good{background:var(--good)}button.warn{background:#c77a08}button.ghost{background:transparent;color:var(--brand);padding:5px 7px}button.link-button{padding:0;background:none;color:var(--brand);font-weight:650;margin:0}button:disabled{opacity:.52;cursor:not-allowed}.row{display:flex;gap:10px;align-items:end}.row>*{flex:1}.action-row{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.action-row button{margin-top:9px}.section-head{display:flex;align-items:flex-start;justify-content:space-between;gap:14px;margin-bottom:10px}.section-head>div:last-child{flex:0 0 auto}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse;margin-top:8px}th,td{text-align:left;padding:10px 8px;border-bottom:1px solid var(--line);vertical-align:middle;white-space:nowrap}th{color:var(--muted);font-size:12px;font-weight:650}.status{font-weight:700}.status.SUCCEEDED,.status.ACTIVE,.status.PROCESSED,.status.POSTED,.status.APPROVED{color:var(--good)}.status.PROCESSING,.status.PENDING,.status.PENDING_APPROVAL,.status.WAITING{color:var(--warn)}.status.FAILED,.status.REJECTED,.status.REJECTED_BUDGET,.status.MANUAL_REVIEW{color:var(--bad)}.empty{color:var(--muted);padding:18px 0}.result{border:1px solid var(--line);background:#fbfcff;border-radius:10px;padding:14px;margin-top:14px}.result.success{border-color:#b9e4cd;background:var(--good-soft)}.result.warning{border-color:#efd39b;background:var(--warn-soft)}.result.error{border-color:#efc0c8;background:var(--bad-soft)}.result-title{font-weight:750;font-size:16px;margin-bottom:6px}.result-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:7px 18px;margin:10px 0}.result-item span{display:block;color:var(--muted);font-size:12px}.result-item strong{display:block;overflow-wrap:anywhere}.copyable{cursor:pointer;color:var(--brand);font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px}.api-details{margin-top:12px;border-top:1px solid var(--line);padding-top:10px}.api-details summary{cursor:pointer;color:var(--brand);font-weight:650}.api-details pre,pre.raw{margin:9px 0 0;background:#111827;color:#d7e1ff;border-radius:9px;padding:14px;overflow:auto;max-height:280px;font:12px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace}.error-panel{border:1px solid #efc0c8;background:var(--bad-soft);border-radius:10px;padding:14px;margin-top:12px}.error-panel .error-title{font-weight:760;color:#9d2e45;margin-bottom:8px}.error-panel dl{display:grid;grid-template-columns:105px 1fr;gap:4px;margin:0}.error-panel dt{color:var(--muted)}.error-panel dd{margin:0;overflow-wrap:anywhere}.hero{background:#fff;border:1px solid var(--line);border-radius:16px;padding:26px 27px;margin-bottom:16px}.hero h2{max-width:690px}.hero p{color:var(--muted);max-width:700px;font-size:15px}.hero.compact{padding:20px 22px}.scenario{display:flex;flex-direction:column;gap:10px;min-height:245px}.scenario .scenario-icon{font-size:25px}.scenario .flow{margin-top:auto}.flow{display:flex;align-items:center;gap:7px;flex-wrap:wrap;color:#4d5b74;font-size:12px}.flow span{border:1px solid var(--line);background:#f8faff;border-radius:7px;padding:6px 8px}.flow i{color:#9aa5b8;font-style:normal}.steps{display:flex;gap:7px;overflow:auto;padding:2px 0 9px;margin-bottom:14px}.step{display:flex;align-items:center;gap:7px;min-width:max-content;color:#8290a7;font-size:12px}.step:not(:last-child):after{content:"→";color:#b3bdcd;margin-left:4px}.step-index{display:grid;place-items:center;width:23px;height:23px;border:1px solid #cbd4e3;border-radius:50%;font-weight:750;background:#fff}.step.done{color:var(--good)}.step.done .step-index{background:var(--good-soft);border-color:#9fd8bb;color:var(--good)}.step.current{color:var(--brand);font-weight:700}.step.current .step-index{background:var(--brand-soft);border-color:#aeb8ff;color:var(--brand)}.step.blocked .step-index{background:#f2f4f8}.timeline{position:relative;margin:16px 0 2px}.timeline:before{content:"";position:absolute;left:9px;top:9px;bottom:9px;width:2px;background:#e1e6ef}.timeline-item{position:relative;display:grid;grid-template-columns:20px 1fr;gap:12px;padding-bottom:19px}.timeline-dot{width:19px;height:19px;border-radius:50%;background:#fff;border:3px solid #bdc7d7;z-index:1}.timeline-item.good .timeline-dot{border-color:var(--good);background:var(--good-soft)}.timeline-item.warn .timeline-dot{border-color:#d89221;background:var(--warn-soft)}.timeline-item.bad .timeline-dot{border-color:var(--bad);background:var(--bad-soft)}.timeline-title{font-weight:720}.timeline-meta{display:flex;gap:10px;flex-wrap:wrap;color:var(--muted);font-size:12px;margin-top:3px}.timeline-detail{color:var(--muted);font-size:12px;margin-top:3px}.drawer{border:1px solid var(--line);background:#fbfcff;border-radius:12px;padding:16px;margin-top:16px}.drawer-close{float:right}.debug-chain{display:flex;gap:0;overflow:auto;margin:15px 0 4px;padding-bottom:4px}.chain-node{min-width:150px;border:1px solid var(--line);background:#fff;padding:11px;border-radius:9px;position:relative}.chain-node:not(:last-child){margin-right:28px}.chain-node:not(:last-child):after{content:"→";position:absolute;right:-22px;top:31px;color:#98a4b8;font-size:20px}.chain-node strong{display:block;font-size:13px}.chain-node span{display:block;color:var(--muted);font-size:12px;margin-top:4px}.debug-block{border-top:1px solid var(--line);padding-top:13px;margin-top:14px}.debug-entry{border:1px solid var(--line);border-radius:9px;padding:12px;margin:8px 0;background:#fff}.debug-entry-head{display:flex;justify-content:space-between;gap:12px;align-items:start}.debug-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-top:8px}.debug-grid div{background:#f8faff;padding:8px;border-radius:7px}.debug-grid span{display:block;color:var(--muted);font-size:11px}.debug-grid strong{display:block;overflow-wrap:anywhere;font-size:12px}.modal-backdrop{position:fixed;inset:0;background:#15203a66;display:grid;place-items:center;padding:20px;z-index:10}.modal{max-width:510px;width:100%;background:#fff;border-radius:14px;padding:21px;box-shadow:0 14px 45px #16203a30}.modal h3{margin-bottom:15px}.confirm-list{border:1px solid var(--line);border-radius:9px;padding:11px;margin:10px 0 14px}.confirm-row{display:flex;justify-content:space-between;gap:12px;padding:6px 0;border-bottom:1px solid #edf0f5}.confirm-row:last-child{border-bottom:0}.confirm-row span{color:var(--muted)}.right{text-align:right}.vcc-summary{background:#f8faff;border-radius:9px;padding:12px;margin-top:12px}.pill-row{display:flex;gap:8px;flex-wrap:wrap}.pill{padding:6px 9px;border-radius:7px;background:#eef1f7;color:#53617a;font-size:12px}.pill.good{background:var(--good-soft);color:var(--good)}.pill.warn{background:var(--warn-soft);color:var(--warn)}
    @media(max-width:960px){.side{width:205px;flex-basis:205px}.main{padding:24px 20px}.half,.third{grid-column:span 12}.env{justify-content:flex-start}.top{display:block}.env{margin-top:10px}.debug-grid,.role-guide-grid{grid-template-columns:1fr 1fr}}
    @media(max-width:650px){.side{width:62px;flex-basis:62px;padding:18px 8px}.brand{font-size:0;padding:0 9px 20px}.brand:after{content:"D";font-size:20px}.sub,.side-note{display:none}.nav{font-size:0;text-align:center;padding:11px 4px}.nav:before{content:"•";font-size:20px}.main{padding:18px 13px}.row{display:block}.row button{width:100%}.result-grid,.debug-grid,.role-guide-grid{grid-template-columns:1fr}.card{padding:16px}.title{font-size:23px}}
  </style>
  <style>
    .conversation{display:grid;gap:7px;margin:9px 0 12px}.conversation-item{padding:8px 10px;border-radius:8px;font-size:12px;max-width:94%}.conversation-item.user{background:#eef1ff;justify-self:end}.conversation-item.assistant{background:#f1f4f8;color:#53617a}.scope-list{margin:9px 0 0;padding-left:18px;color:#53617a;font-size:12px}.clarify-box{border:1px dashed #d4b36b;background:#fffaf0;border-radius:8px;padding:10px;margin-top:10px}.clarify-box strong{display:block;margin-bottom:4px}.role-guide{background:#f1f5ff;border:1px solid #dce4ff;border-radius:12px;padding:14px 16px;margin-bottom:16px}.role-guide-head{display:flex;align-items:baseline;gap:8px;flex-wrap:wrap}.role-guide-title{font-weight:760;font-size:16px}.role-guide-role{font-size:12px;color:var(--brand);font-weight:700}.role-guide-grid{display:grid;grid-template-columns:1fr 1fr 1.25fr;gap:14px;margin-top:9px}.role-guide-label{font-size:11px;color:var(--muted);margin-bottom:2px}.role-guide-value{font-weight:650}.role-guide details{margin-top:10px;font-size:12px}.role-guide summary{cursor:pointer;color:var(--brand);font-weight:650}.term-list{display:flex;gap:6px;flex-wrap:wrap;margin-top:7px}.term{background:#fff;border:1px solid #dce4ff;border-radius:6px;padding:4px 7px;color:#53617a}.term b{color:var(--ink)}
  </style>
  <style>
    .persona-switch{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;margin-bottom:16px;padding:10px 12px;background:#fff;border:1px solid var(--line);border-radius:11px}
    .persona-switch-label{font-weight:750}.persona-tabs{display:flex;gap:6px;flex-wrap:wrap}.persona-tab{margin:0;padding:7px 11px;background:#eef1f7;color:#53617a}.persona-tab.active{background:var(--brand);color:#fff}.role-separation{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px;margin:12px 0}.role-separation div{border:1px solid var(--line);border-radius:9px;padding:10px;background:#fbfcff}.role-separation strong{display:block;margin-bottom:3px}.business-flow{display:flex;gap:7px;flex-wrap:wrap;align-items:center;margin:13px 0}.business-flow span{padding:7px 9px;border-radius:7px;background:#f8faff;border:1px solid var(--line);font-size:12px}.business-flow i{font-style:normal;color:#98a4b8}.reliability-lab{margin-top:14px;border:1px dashed #d8c184;border-radius:10px;background:#fffaf0;padding:10px 12px}.reliability-lab summary{cursor:pointer;color:#865b12;font-weight:750}.reliability-lab[open] summary{margin-bottom:9px}.lab-note{color:#80520d;font-size:12px;margin:2px 0 8px}.embedded-lab{margin-top:14px}.webhook-next{margin-top:14px;padding:13px 14px;border:1px solid #b9e4cd;border-radius:10px;background:var(--good-soft)}.webhook-next-title{font-weight:760;color:#126d4b;font-size:15px}.webhook-next p{margin:5px 0 2px;color:#487463;font-size:12px}.webhook-next button{margin-top:9px}.vcc-builder{border:1px solid #dce4ff;background:#f8faff;border-radius:10px;padding:13px;margin:10px 0 12px}.vcc-builder-title{font-weight:760;font-size:15px}.vcc-builder-grid{display:grid;grid-template-columns:1.2fr 1fr 1fr 1fr;gap:9px}.vcc-preview{border:1px solid #dce4ff;background:#fff;border-radius:8px;padding:10px;margin-top:10px;font-weight:650;color:#34405b}.vcc-builder-status{font-size:12px;color:var(--muted);margin-top:7px}.advanced-request{margin-top:10px}.advanced-request summary{cursor:pointer;color:var(--brand);font-weight:650}.advanced-request textarea{margin-top:8px}.persona-empty{border:1px dashed #cbd4e3;border-radius:10px;padding:16px;background:#fbfcff;color:var(--muted)}
    @media(max-width:650px){.role-separation{grid-template-columns:1fr}.vcc-builder-grid{grid-template-columns:1fr}}
  </style>
</head>
<body>
<div class="shell">
  <aside class="side">
    <div class="brand">DemoAI</div><div class="sub">支付集成演示</div>
    <a class="nav" data-page="demo" href="/ui/demo">上手演示</a>
    <a class="nav" data-page="developer" href="/ui/developer">开发者：充值</a>
    <a class="nav" data-page="finance" href="/ui/finance">财务：交易</a>
    <a class="nav" data-page="fde" href="/ui/fde">FDE：排障</a>
    <a class="nav" data-page="vcc" href="/ui/vcc">企业：申请 VCC</a>
    <div class="sub side-note">本地 Mock 环境<br>不处理真实资金<br>Sandbox：待配置</div>
  </aside>
  <main class="main">
    <div class="top"><div><div class="eyebrow">DemoAI · 支付集成实验室</div><div class="title">{{TITLE}}</div></div><div class="env" id="environment"><span class="chip">环境：加载中…</span></div></div>
    <div class="notice small">这是本地 Mock 演示，不处理真实资金。成功状态来自本地 Provider / Webhook 模拟；Mock 已验证不等于 PingPong Sandbox 已联调。</div>
    <div id="app"></div>
  </main>
</div>
<div id="modal-root"></div>
<script>
const PAGE='{{PAGE}}'; const API='/api';
const TOKENS={developer:'dev-token',finance:'finance-token',approver:'approver-token',admin:'admin-token',fde:'fde-token'};
let lastApi=null; let confirmHandler=null; let currentPayment=null; let currentVcc={applicationId:null, parsed:null, data:null, periodExplicit:false}; let vccConversation=[];
function token(role){return localStorage.getItem('demo-token-'+role)||TOKENS[role]}
function auth(role){return {'Authorization':'Bearer '+token(role)}}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function money(v,currency='USD'){return v==null||v===''?'待补充':Number(v).toFixed(2)+' '+currency}
function fmtTime(v){if(!v)return '—';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):d.toLocaleString('zh-CN',{hour12:false})}
function statusText(v){return ({SUCCEEDED:'已成功',PROCESSING:'处理中',CREATED:'已创建',FAILED:'失败',CANCELLED:'已取消',REVIEW_REQUIRED:'待人工复核',PENDING_APPROVAL:'待审批',APPROVED:'已批准',ACTIVE:'已激活',REJECTED:'已拒绝',POSTED:'已入账',PROCESSED:'已处理',MANUAL_REVIEW:'人工复核',WAITING:'等待中'}[v]||v||'—')}
function statusClass(v){return ['SUCCEEDED','ACTIVE','PROCESSED','POSTED','APPROVED'].includes(v)?'good':['PROCESSING','PENDING','PENDING_APPROVAL','WAITING','REVIEW_REQUIRED'].includes(v)?'warn':['FAILED','CANCELLED','REJECTED','REJECTED_BUDGET','MANUAL_REVIEW'].includes(v)?'bad':''}
function setApp(html){document.getElementById('app').innerHTML=html}
function copyText(value){if(!value)return;const done=()=>toast('已复制');if(navigator.clipboard){navigator.clipboard.writeText(value).then(done).catch(()=>toast('复制失败，请手动选择'))}else{const input=document.createElement('textarea');input.value=value;document.body.appendChild(input);input.select();document.execCommand('copy');input.remove();done()}}
function copyButton(value){return value?`<button class="ghost copyable" onclick="copyText('${esc(value)}')" title="复制">${esc(value)}</button>`:''}
function json(value){return esc(JSON.stringify(value??{},null,2))}
function table(rows,headers,render){if(!rows||!rows.length)return '<div class="empty">暂无记录</div>';return '<div class="table-wrap"><table><thead><tr>'+headers.map(x=>'<th>'+x+'</th>').join('')+'</tr></thead><tbody>'+rows.map(render).join('')+'</tbody></table></div>'}
function rawDetails(label,value){return `<details class="api-details"><summary>${label||'查看 API 详情'}</summary><pre>${json(value)}</pre></details>`}
function toast(message){let el=document.getElementById('toast');if(!el){el=document.createElement('div');el.id='toast';el.className='toast';document.body.appendChild(el)}el.textContent=message;clearTimeout(window.__toastTimer);window.__toastTimer=setTimeout(()=>el.remove(),3500)}
function traceId(){return 'ui-'+Date.now().toString(36)+'-'+Math.random().toString(16).slice(2,10)}
function errorObject(message,status,body,trace){return {message:String(message||'请求失败'),status,body,traceId:trace}}
async function api(path,options={}){const headers={...(options.headers||{})};const trace=headers['X-Trace-Id']||traceId();headers['X-Trace-Id']=trace;const safeOptions={...options,headers};const bodyForLog=options.body;const response=await fetch(API+path,safeOptions);let data={};try{data=await response.json()}catch{};lastApi={path,method:options.method||'GET',requestBody:bodyForLog?JSON.parse(bodyForLog):null,response:data,status:response.status,traceId:trace};if(!response.ok){const detail=typeof data.detail==='string'?data.detail:(data.detail?.message||`HTTP ${response.status}`);throw errorObject(detail,response.status,data,trace)}return data}
function errorPanel(err,context){const message=err?.message||String(err||'操作失败');let advice='请检查输入后重试；如果状态未知，先使用主动查单，不要创建新的交易。';if(/not refundable|PROCESSING|refundable/i.test(message))advice='当前 Payment 还没有进入可退款的终态，建议先执行主动查单。';if(/approval|approved|human/i.test(message))advice='请完成前置审批和人工确认，再继续开卡。';if(/budget/i.test(message))advice='请降低金额或调整预算后重新提交。';return `<div class="error-panel"><div class="error-title">${esc(context||'操作失败')}</div><dl><dt>发生了什么</dt><dd>${esc(message)}</dd><dt>为什么失败</dt><dd>${esc(err?.status?`API 返回 HTTP ${err.status}`:'请求未完成')}</dd><dt>下一步建议</dt><dd>${esc(advice)}</dd>${lastApi?.path?`<dt>请求</dt><dd>${esc(lastApi.method+' '+lastApi.path)}</dd>`:''}<dt>Trace ID</dt><dd>${copyButton(err?.traceId||lastApi?.traceId)||'—'}</dd></dl></div>`}
function mockScenarioOptions(){return [['PENDING','PENDING：等待 Webhook'],['SUCCESS','SUCCESS：创建即成功'],['FAIL','FAIL：Provider 拒绝'],['CLOSE','CLOSE：Provider 关闭'],['AUTH_SUCCESS','AUTH_SUCCESS：人工复核'],['TIMEOUT','TIMEOUT：结果未知'],['429','429：限流重试']].map(x=>`<option value="${x[0]}">${x[1]}</option>`).join('')}
async function applyMockScenario(scenario,button){return runAction(button,'应用中…',async()=>{const d=await api('/admin/mock/scenario',{method:'POST',headers:{...auth('admin'),'Content-Type':'application/json'},body:JSON.stringify({scenario})});toast('Mock 场景已切换：'+d.scenario);return d})}
async function selectMockScenario(scenario,button,targetId){const target=document.getElementById(targetId||'finance-feedback');try{const d=await applyMockScenario(scenario,button);if(target){target.className='result success';target.innerHTML=`<div class="result-title">Mock Provider 场景已应用</div><p>当前场景：<strong>${esc(d.scenario||scenario)}</strong>。它会影响下一笔充值或后续 Mock 操作，不会修改已有交易。</p>`}await loadFinance()}catch(e){if(target)target.innerHTML=errorPanel(e,'Mock Provider 场景应用失败')}}
async function runAction(button,busy,fn){if(button){button.disabled=true;button.dataset.original=button.textContent;delete button.dataset.keepDisabled;delete button.dataset.doneText;button.textContent=busy}try{return await fn()}finally{if(button){const keepDisabled=button.dataset.keepDisabled==='true';button.disabled=keepDisabled;button.textContent=keepDisabled?(button.dataset.doneText||button.dataset.original):button.dataset.original;delete button.dataset.keepDisabled;delete button.dataset.doneText}}}
async function loadEnvironment(){try{const d=await fetch('/health').then(r=>r.json());const mock=d.pingpong_mode==='mock';const mode=(d.pingpong_mode||'UNCONFIGURED').toUpperCase();document.getElementById('environment').innerHTML=`<span class="chip ${mock?'good':'warn'}">环境：${esc(mode)}</span><span class="chip good">接口契约：已验证</span>${mock?'<span class="chip good">Mock E2E：已验证</span>':'<span class="chip warn">Mock E2E：未启用</span>'}<span class="chip warn">Sandbox：等待供应商配置</span>`}catch(e){document.getElementById('environment').innerHTML='<span class="chip warn">环境：暂不可用</span>'}}
function openConfirm(config){confirmHandler={action:config.onConfirm,sourceButton:config.sourceButton};document.getElementById('modal-root').innerHTML=`<div class="modal-backdrop" role="dialog" aria-modal="true"><div class="modal"><h3>${esc(config.title)}</h3><p class="muted">${esc(config.description||'请确认后继续。')}</p><div class="confirm-list">${(config.rows||[]).map(row=>`<div class="confirm-row"><span>${esc(row[0])}</span><strong>${esc(row[1])}</strong></div>`).join('')}</div><div class="right"><button class="secondary" onclick="closeConfirm()">取消</button><button class="${config.danger?'warn':'good'}" onclick="confirmAction(this)">${esc(config.confirmText||'确认执行')}</button></div></div></div>`}
function closeConfirm(){confirmHandler=null;document.getElementById('modal-root').innerHTML=''}
async function confirmAction(button){const config=confirmHandler;closeConfirm();if(config?.action)await runAction(config.sourceButton||button,'执行中…',config.action)}
function roleGuide(role,who,why,next,terms){return `<section class="role-guide"><div class="role-guide-head"><span class="role-guide-title">你现在在这里做什么？</span><span class="role-guide-role">角色：${esc(role)}</span></div><div class="role-guide-grid"><div><div class="role-guide-label">你是谁</div><div class="role-guide-value">${esc(who)}</div></div><div><div class="role-guide-label">为什么使用</div><div class="role-guide-value">${esc(why)}</div></div><div><div class="role-guide-label">下一步</div><div class="role-guide-value">${esc(next)}</div></div></div><details><summary>名词说明</summary><div class="term-list">${(terms||[]).map(item=>`<span class="term"><b>${esc(item[0])}</b>：${esc(item[1])}</span>`).join('')}</div></details></section>`}

function demo(){setApp(roleGuide('首次体验者','客户或刚加入项目的开发者','先理解 DemoAI 的收款和企业支出两个核心场景','选择一个场景，点击“开始演示”即可', [['Credits','给 AI API 用户充值的额度'],['Payment','一笔充值订单及其状态'],['VCC','企业虚拟卡，用于受控支出'],['FDE','负责客户接入与问题排查的工程师']])+`<section class="hero"><div class="eyebrow">5 秒了解这个 POC</div><h2>DemoAI 如何接入跨境收款和企业虚拟卡？</h2><p>这是一个 AI 平台支付集成实验室：开发者为模型 API 用户充值 Credits，财务发起企业 VCC 申请，FDE 沿着 Payment、Webhook、状态机和 Ledger 定位问题。</p><div class="pill-row"><span class="pill good">收款：创建 → 确认 → 入账</span><span class="pill">支出：申请 → 审批 → 开卡</span><span class="pill">Mock 环境，可安全演示</span></div></section><div class="grid"><section class="card half scenario"><div class="scenario-icon">◎</div><div><h3>场景一：AI Credits 收款</h3><p class="muted">为全球开发者充值模型 API Credits，覆盖创建订单、支付通知、主动查单、退款。</p></div><div class="flow"><span>创建充值</span><i>→</i><span>Provider / Webhook</span><i>→</i><span>Credits 入账</span></div><div><a href="/ui/developer?demo=credits"><button class="good">开始演示</button></a><a href="/ui/fde"><button class="secondary">查看调用链</button></a></div></section><section class="card half scenario"><div class="scenario-icon">▣</div><div><h3>场景二：VCC 企业支出</h3><p class="muted">用自然语言申请 AWS / SaaS 支出，经过预算、角色权限、人工审批后创建 Mock VCC。</p></div><div class="flow"><span>申请</span><i>→</i><span>预算 / 审批</span><i>→</i><span>开卡</span></div><div><a href="/ui/vcc?demo=vcc"><button class="good">开始演示</button></a><a href="/ui/finance"><button class="secondary">进入财务页面</button></a></div></section><section class="card"><h3>三个入口分别解决什么问题？</h3><div class="grid"><div class="third"><strong>开发者</strong><p class="muted small">创建充值，查看状态和 Credits 是否入账。</p></div><div class="third"><strong>财务</strong><p class="muted small">查看交易，执行退款，发起企业用卡流程。</p></div><div class="third"><strong>FDE</strong><p class="muted small">用 ID 串起调用链，快速定位异常。</p></div></div></section></div>`)}

async function developer(){setApp(roleGuide('开发者（Developer）','负责 AI 平台接入或充值体验的开发者','验证用户充值能否创建、确认并最终入账 Credits','先选择 Provider 场景 → 创建充值 → 触发 Webhook / 主动查单 → 查看 Timeline', [['Credits','AI API 可消费的额度'],['Payment','充值订单，状态不会只由前端决定'],['Webhook','Provider 发来的支付状态通知'],['失败实验','Mock 环境中可重复触发失败、超时和限流']])+`<section class="hero compact"><div class="eyebrow">客户场景 · AI Credits 收款</div><h2>给模型 API 用户充值 Credits</h2><p>你正在模拟开发者充值链路。除了成功 Webhook，也可以直接触发 Provider 拒绝、关闭、人工复核、超时和限流，观察失败恢复路径。</p></section><div class="grid"><section class="card third"><div class="muted">可用 Credits</div><div class="metric" id="balance">—</div><div class="muted small">Credits 单位：USD_CREDIT</div></section><section class="card half"><div class="section-head"><div><h3>创建充值订单</h3><div class="muted small">Mock 场景只影响本地演示，不代表真实 Sandbox 行为。</div></div></div><div class="row"><div><label for="amount">充值金额（USD）</label><input id="amount" value="100.00" inputmode="decimal"></div><div><label for="payment-scenario">Provider 创建场景</label><select id="payment-scenario"><option value="PENDING">PENDING：等待 Webhook</option><option value="SUCCESS">SUCCESS：创建即成功</option><option value="FAIL">FAIL：Provider 拒绝</option><option value="CLOSE">CLOSE：Provider 关闭</option><option value="AUTH_SUCCESS">AUTH_SUCCESS：人工复核</option><option value="TIMEOUT">TIMEOUT：结果未知</option><option value="429">429：限流重试</option></select></div></div><button onclick="topup(this)">创建充值订单</button><div id="topup-result" class="result"><div class="muted">提交后会显示充值结果、下一步动作和 Payment ID。</div></div></section><section class="card"><div class="section-head"><div><h3>充值记录</h3><div class="muted small">每行可直接查看详情，处理中订单可主动查单。</div></div><button class="secondary" onclick="refreshDeveloper(this)">刷新记录</button></div><div id="payments">加载中…</div><div id="payment-detail"></div></section></div>`);await refreshDeveloper();const query=new URLSearchParams(location.search);if(query.get('demo')==='credits')document.getElementById('amount')?.focus()}
async function refreshDeveloper(button){await runAction(button,'刷新中…',async()=>{try{const b=await api('/me/credits',{headers:auth('developer')});document.getElementById('balance').textContent=money(b.available_balance);const rows=await api('/payments',{headers:auth('developer')});document.getElementById('payments').innerHTML=paymentTable(rows,'developer')}catch(e){document.getElementById('payments').innerHTML=errorPanel(e,'充值记录加载失败')}})}
function paymentTable(rows,role){return table(rows,['支付订单（Payment ID）','金额','本地状态','Provider 状态','操作'],r=>`<tr><td>${copyButton(r.payment_id)}</td><td>${money(r.amount,r.currency)}</td><td class="status ${statusClass(r.payment_status)}">${esc(statusText(r.payment_status))}</td><td>${esc(r.provider_status||'—')}</td><td><div class="action-row"><button class="link-button" onclick="openPaymentDetail('${esc(r.payment_id)}','${role}')">查看详情</button>${r.payment_status==='PROCESSING'?`<button class="link-button" onclick="queryPayment('${esc(r.payment_id)}',this,'${role}')">主动查单</button>`:''}${role==='finance'&&r.payment_status==='SUCCEEDED'&&!r.refunded?`<button class="link-button" onclick="startRefund('${esc(r.payment_id)}',this)">发起退款</button>`:''}</div></td></tr>`)}
async function topup(button){const amount=document.getElementById('amount').value;if(!amount||Number(amount)<=0){document.getElementById('topup-result').innerHTML=errorPanel({message:'请输入大于 0 的金额'},'充值金额无效');return}openConfirm({sourceButton:button,title:'确认创建充值订单？',description:'这会创建一笔 Payment。Mock 演示不处理真实资金，但仍按真实资金动作的确认习惯展示对象和金额。',rows:[['操作对象','DemoAI Credits'],['充值金额',money(amount,'USD')],['当前状态','尚未创建'],['操作影响','创建 PaymentOrder，等待 Provider / Webhook 确认']],confirmText:'确认创建订单',onConfirm:async()=>executeTopup(amount)})}
async function executeTopup(amount){const target=document.getElementById('topup-result');target.innerHTML='<div class="muted">处理中…正在创建 Payment。</div>';try{const d=await api('/topups',{method:'POST',headers:{...auth('developer'),'Content-Type':'application/json','Idempotency-Key':'ui-topup-'+Date.now()},body:JSON.stringify({amount,currency:'USD'})});currentPayment=d.payment_id;renderPaymentResult(target,d,{amount,currency:'USD'},'topup');await refreshDeveloper()}catch(e){target.innerHTML=errorPanel(e,'充值订单创建失败')}}
function renderPaymentResult(target,d,request,kind){const status=d.payment_status||d.status;const success=status==='SUCCEEDED';target.className='result '+(success?'success':status==='FAILED'?'error':'warning');target.innerHTML=`<div class="result-title">${success?'充值已成功':status==='PROCESSING'?'订单已创建，等待 Provider 确认':'充值结果：'+esc(statusText(status))}</div><div class="result-grid"><div class="result-item"><span>金额</span><strong>${money(request.amount,request.currency)}</strong></div><div class="result-item"><span>支付订单 ID（Payment ID）</span><strong>${copyButton(d.payment_id)}</strong></div><div class="result-item"><span>当前状态</span><strong class="status ${statusClass(status)}">${esc(statusText(status))}</strong></div><div class="result-item"><span>下一步</span><strong>${status==='PROCESSING'?'等待 Webhook，或打开详情主动查单':success?'Credits 已通过 Ledger 入账（只入账一次）':'查看 API 详情并按建议恢复'}</strong></div></div>${status==='PROCESSING'?'<div class="action-row"><button class="good" onclick="mockSuccess(\''+esc(d.payment_id)+'\',\''+esc(request.amount)+'\',this)">模拟 SUCCESS Webhook</button><button class="secondary" onclick="openPaymentDetail(\''+esc(d.payment_id)+'\',\'developer\')">查看支付状态时间线</button></div>':''}${rawDetails('查看 API 详情',lastApi)}`}
async function mockSuccess(paymentId,amount,button){await runAction(button,'处理中…',async()=>{try{const body=JSON.stringify({partner_transaction_id:'txn_'+paymentId,status:'SUCCESS',amount,currency:'USD'});const d=await api('/webhooks/pingpong/checkout',{method:'POST',headers:{'Content-Type':'application/json','X-Mock-Signature':await hmac(body)},body});toast('SUCCESS Webhook 已处理');const target=document.getElementById('topup-result');if(target){target.className='result success';target.innerHTML=`<div class="result-title">充值已成功</div><div class="result-grid"><div class="result-item"><span>支付订单 ID（Payment ID）</span><strong>${copyButton(paymentId)}</strong></div><div class="result-item"><span>当前状态</span><strong class="status good">已成功（SUCCEEDED）</strong></div><div class="result-item"><span>下一步</span><strong>Credits 已通过 Ledger 入账（只入账一次）</strong></div></div>${rawDetails('查看 API 详情',d)}</div>`}await refreshDeveloper()}catch(e){const target=document.getElementById('topup-result');if(target)target.innerHTML=errorPanel(e,'Webhook 处理失败')}})}

async function topup(button){const amount=document.getElementById('amount').value;const scenario=document.getElementById('payment-scenario')?.value||'PENDING';if(!amount||Number(amount)<=0){document.getElementById('topup-result').innerHTML=errorPanel({message:'请输入大于 0 的金额'},'充值金额无效');return}openConfirm({sourceButton:button,title:'确认创建充值订单？',description:'这会创建一笔 Payment。Mock 演示不处理真实资金，但仍按真实资金动作的确认习惯展示对象和金额。',rows:[['操作对象','DemoAI Credits'],['充值金额',money(amount,'USD')],['Provider 场景',scenario],['当前状态','尚未创建'],['操作影响','创建 PaymentOrder，并观察 Provider 成功、失败或未知结果']],confirmText:'确认创建订单',onConfirm:async()=>executeTopup(amount,scenario)})}
async function executeTopup(amount,scenario){const target=document.getElementById('topup-result');target.innerHTML='<div class="muted">处理中…正在设置 Provider 场景并创建 Payment。</div>';try{await applyMockScenario(scenario);const d=await api('/topups',{method:'POST',headers:{...auth('developer'),'Content-Type':'application/json','Idempotency-Key':'ui-topup-'+Date.now()},body:JSON.stringify({amount,currency:'USD'})});currentPayment=d.payment_id;renderPaymentResult(target,d,{amount,currency:'USD'},'topup');await refreshDeveloper()}catch(e){target.innerHTML=errorPanel(e,'充值订单创建失败')}}
function webhookScenarioOptions(){return [['PENDING','PENDING：仍在处理中'],['SUCCESS','SUCCESS：支付成功'],['FAIL','FAIL：支付失败'],['CLOSE','CLOSE：支付关闭'],['AUTH_SUCCESS','AUTH_SUCCESS：转人工复核'],['UNKNOWN','UNKNOWN：非法 Provider 状态']].map(x=>`<option value="${x[0]}">${x[1]}</option>`).join('')}
function renderPaymentResult(target,d,request,kind,details=lastApi){const status=d.payment_status||d.status;const success=status==='SUCCEEDED';const failure=['FAILED','CANCELLED'].includes(status);const title=success?'充值已成功':status==='PROCESSING'?'订单已创建，等待 Provider 确认':status==='REVIEW_REQUIRED'?'支付已转人工复核':failure?'充值未成功：'+statusText(status):'充值结果：'+esc(statusText(status));const next=success?'Credits 已通过 Ledger 入账（只入账一次）':status==='PROCESSING'?'选择一种 Webhook 结果，或打开详情执行主动查单':status==='REVIEW_REQUIRED'?'等待 Provider / FDE 完成人工复核；Credits 不入账':failure?'Provider 未成功，Credits 不入账；请查看 Timeline 和失败原因':'查看 API 详情并按建议恢复';const webhookControls=status==='PROCESSING'?`<div class="action-row"><div><label for="webhook-scenario">Webhook 测试结果</label><select id="webhook-scenario">${webhookScenarioOptions()}</select></div><button class="good" onclick="mockWebhook('${esc(d.payment_id)}','${esc(request.amount)}',this)">发送 Webhook</button><button class="secondary" onclick="mockWebhook('${esc(d.payment_id)}','${esc(request.amount)}',this,'INVALID_SIGNATURE',true)">验签失败测试</button></div>`:'';target.className='result '+(success?'success':failure?'error':'warning');target.innerHTML=`<div class="result-title">${title}</div><div class="result-grid"><div class="result-item"><span>金额</span><strong>${money(request.amount,request.currency)}</strong></div><div class="result-item"><span>支付订单 ID（Payment ID）</span><strong>${copyButton(d.payment_id)}</strong></div><div class="result-item"><span>当前状态</span><strong class="status ${statusClass(status)}">${esc(statusText(status))}</strong></div><div class="result-item"><span>下一步</span><strong>${next}</strong></div></div>${webhookControls}<div class="action-row"><button class="secondary" onclick="openPaymentDetail('${esc(d.payment_id)}','developer')">查看支付状态时间线</button>${status==='PROCESSING'?`<button class="secondary" onclick="queryPayment('${esc(d.payment_id)}',this,'developer')">主动查单</button>`:''}</div>${rawDetails('查看 API 详情',details)}`}
async function mockWebhook(paymentId,amount,button,forcedStatus,invalidSignature=false){const status=forcedStatus||document.getElementById('webhook-scenario')?.value||'SUCCESS';await runAction(button,'处理中…',async()=>{const body=JSON.stringify({partner_transaction_id:'txn_'+paymentId,status,amount,currency:'USD'});try{const signature=invalidSignature?'invalid':await hmac(body);const webhookResponse=await api('/webhooks/pingpong/checkout',{method:'POST',headers:{'Content-Type':'application/json','X-Mock-Signature':signature},body});const payment=await api('/payments/'+encodeURIComponent(paymentId),{headers:auth('developer')});const target=document.getElementById('topup-result');if(target)renderPaymentResult(target,payment,{amount,currency:'USD'},'webhook',webhookResponse);toast(invalidSignature?'Webhook 验签失败，订单状态保持不变':'Webhook '+status+' 已处理');await refreshDeveloper()}catch(e){const target=document.getElementById('topup-result');if(target)target.innerHTML=errorPanel(e,invalidSignature?'Webhook 验签失败':'Webhook 处理失败')}})}
async function mockSuccess(paymentId,amount,button){return mockWebhook(paymentId,amount,button,'SUCCESS',false)}

async function openPaymentDetail(paymentId,role){const target=document.getElementById('payment-detail');if(!target)return;target.innerHTML='<div class="drawer">正在加载支付详情和状态时间线…</div>';try{const authRole=role==='finance'?'finance':role==='fde'?'admin':'developer';const [payment,timeline]=await Promise.all([api('/payments/'+encodeURIComponent(paymentId),{headers:auth(authRole)}),api('/payments/'+encodeURIComponent(paymentId)+'/timeline',{headers:auth(authRole)})]);currentPayment=payment;target.innerHTML=`<div class="drawer"><button class="ghost drawer-close" onclick="document.getElementById('payment-detail').innerHTML=''">关闭</button><h3>支付详情（Payment）</h3><div class="result-grid"><div class="result-item"><span>支付订单 ID（Payment ID）</span><strong>${copyButton(payment.payment_id)}</strong></div><div class="result-item"><span>金额</span><strong>${money(payment.amount,payment.currency)}</strong></div><div class="result-item"><span>支付状态</span><strong class="status ${statusClass(payment.payment_status)}">${esc(statusText(payment.payment_status))}</strong></div><div class="result-item"><span>Provider 状态（服务商状态）</span><strong>${esc(payment.provider_status||'—')}</strong></div><div class="result-item"><span>Provider 请求 ID</span><strong>${copyButton(payment.provider_request_id)}</strong></div><div class="result-item"><span>Provider 交易 ID</span><strong>${copyButton(payment.provider_transaction_id)}</strong></div></div><div class="action-row">${payment.payment_status==='PROCESSING'?`<button class="secondary" onclick="queryPayment('${esc(payment.payment_id)}',this,'${role}')">主动查单</button>`:''}${role==='finance'&&payment.payment_status==='SUCCEEDED'&&!payment.refunded?`<button class="warn" onclick="startRefund('${esc(payment.payment_id)}',this)">发起退款</button>`:''}</div><h4 style="margin-top:18px">支付状态时间线（Payment Timeline）</h4>${renderTimeline(timeline.events)}${rawDetails('查看 API 详情',timeline)}</div>`}catch(e){target.innerHTML=errorPanel(e,'支付详情加载失败')}}
function renderTimeline(events){if(!events||!events.length)return '<div class="empty">暂无状态事件</div>';return '<div class="timeline">'+events.map(e=>`<div class="timeline-item ${statusClass(e.status)}"><div class="timeline-dot"></div><div><div class="timeline-title">${esc(e.title)}</div><div class="timeline-meta"><span>${fmtTime(e.at)}</span>${e.status?`<span>状态：${esc(statusText(e.status))}</span>`:''}${e.provider_status?`<span>Provider：${esc(e.provider_status)}</span>`:''}${e.ledger_result?`<span>Ledger：${esc(e.ledger_result)}</span>`:''}${e.trace_id?`<span>Trace ID：${copyButton(e.trace_id)}</span>`:''}</div>${e.detail?`<div class="timeline-detail">${esc(e.detail)}</div>`:''}</div></div>`).join('')+'</div>'}
async function queryPayment(paymentId,button,role){await runAction(button,'查单中…',async()=>{try{const r=await api('/payments/'+encodeURIComponent(paymentId)+'/query',{method:'POST',headers:auth(role==='fde'?'admin':role==='finance'?'finance':'developer')});toast('主动查单完成：'+statusText(r.payment_status));if(role==='developer'){await refreshDeveloper()}else if(role==='finance'){await loadFinance()}else{await findFde()}openPaymentDetail(paymentId,role)}catch(e){const target=document.getElementById(role==='developer'?'topup-result':'payment-detail');if(target)target.innerHTML=errorPanel(e,'主动查单失败')}})}

async function finance(){setApp(roleGuide('财务（Finance）','负责交易、退款和企业支出的财务人员','确认收款状态，安全地发起退款，或进入 VCC 申请流程','先查看交易状态 → 选择一行查看详情 → 满足条件后发起退款', [['Payment','一笔充值订单'],['Refund','对已成功 Payment 的全额退款'],['Credit Hold','退款前暂时冻结的 Credits'],['审批','高金额 VCC 必须由 Approver 人工批准']])+`<section class="hero compact"><div class="eyebrow">运营场景 · 收款与企业支出</div><h2>管理交易、退款和企业用卡</h2><p>先确认当前 Payment 状态，再对满足条件的订单发起全额退款。退款和开卡都会展示影响并要求二次确认。</p></section><div class="grid"><section class="card"><div class="section-head"><div><h3>交易记录</h3><div class="muted small">操作会自动携带当前行的 Payment ID，不需要手动复制。</div></div><button class="secondary" onclick="loadFinance(this)">刷新记录</button></div><div id="finance-payments">加载中…</div><div id="finance-feedback"></div><div id="payment-detail"></div></section><section class="card half"><h3>下一步怎么做？</h3><p class="muted">只有已成功且 Credits 余额足够的 Payment 才能退款；如果仍在处理中，请先主动查单。</p><div class="action-row"><a href="/ui/vcc"><button>申请企业 VCC</button></a><a href="/ui/fde"><button class="secondary">交给 FDE 排查</button></a></div></section></div>`);await loadFinance()}
async function finance(){setApp(roleGuide('财务（Finance）','负责收款对账、退款和异常排查的财务人员','确认收款状态并安全发起退款','查看交易记录 → 操作退款 → 必要时交给 FDE', [['Payment','一笔充值订单'],['Refund','对已成功 Payment 的全额退款'],['Credit Hold','退款前暂时冻结的 Credits'],['失败实验','切换 Mock Provider，验证异常和恢复路径']])+`<section class="hero compact"><div class="eyebrow">运营场景 · 收款运营</div><h2>管理交易与退款</h2><p>先确认当前 Payment 状态，再对满足条件的订单发起全额退款。退款会展示 Credits 影响并要求二次确认。</p></section><div class="grid"><section class="card"><div class="section-head"><div><h3>交易记录</h3><div class="muted small">操作会自动携带当前行的 Payment ID，不需要手动复制。</div></div><button class="secondary" onclick="loadFinance(this)">刷新记录</button></div><div id="finance-payments">加载中…</div><div id="payment-detail"></div></section><section class="card half"><h3>Mock Provider 异常实验室</h3><p class="muted small">切换本地 Provider 行为后，再创建充值或发起退款。它只影响当前 Mock 进程，不代表真实 PingPong Sandbox。</p><div class="row"><div><label for="finance-scenario">Provider 场景</label><select id="finance-scenario">${mockScenarioOptions()}</select></div><button class="secondary" onclick="selectMockScenario(document.getElementById('finance-scenario').value,this,'finance-feedback')">应用 Provider 场景</button></div><div id="finance-feedback"></div><p class="muted small">应用后会保留明确反馈；场景只影响后续 Mock 操作，不会修改已有交易。Webhook 验签、非法状态和金额不匹配可在开发者页面触发。</p><div class="action-row"><a href="/ui/fde"><button class="secondary">交给 FDE 排查</button></a></div></section></div>`);await loadFinance()}
async function loadFinance(button){await runAction(button,'刷新中…',async()=>{try{const rows=await api('/payments',{headers:auth('finance')});document.getElementById('finance-payments').innerHTML=paymentTable(rows,'finance')}catch(e){document.getElementById('finance-payments').innerHTML=errorPanel(e,'交易记录加载失败')}})}
async function startRefund(paymentId,button){try{const payment=await api('/payments/'+encodeURIComponent(paymentId),{headers:auth('finance')});openConfirm({sourceButton:button,title:'确认发起全额退款？',description:'退款会冻结并扣减对应 Credits；这是不可逆的资金相关操作，请确认对象和当前状态。',danger:true,confirmText:'确认退款',rows:[['操作对象',payment.payment_id],['退款金额',money(payment.amount,payment.currency)],['当前状态',statusText(payment.payment_status)],['操作影响','创建 RefundOrder 并调用 Provider']],onConfirm:async()=>executeRefund(payment)})}catch(e){const target=document.getElementById('finance-feedback');if(target)target.innerHTML=errorPanel(e,'退款前置检查失败')}}
async function executeRefund(payment){const target=document.getElementById('finance-feedback');target.innerHTML='<div class="result warning">处理中…正在创建退款订单（RefundOrder）。</div>';try{const d=await api('/refunds',{method:'POST',headers:{...auth('finance'),'Content-Type':'application/json','Idempotency-Key':'ui-refund-'+payment.payment_id},body:JSON.stringify({payment_id:payment.payment_id})});target.innerHTML=`<div class="result ${d.refund_status==='SUCCEEDED'?'success':'warning'}"><div class="result-title">退款${d.refund_status==='SUCCEEDED'?'已成功':'已提交'}</div><div class="result-grid"><div class="result-item"><span>退款 ID（Refund ID）</span><strong>${copyButton(d.refund_id)}</strong></div><div class="result-item"><span>金额</span><strong>${money(payment.amount,payment.currency)}</strong></div><div class="result-item"><span>当前状态</span><strong class="status ${statusClass(d.refund_status)}">${esc(statusText(d.refund_status))}</strong></div><div class="result-item"><span>支付订单 ID</span><strong>${copyButton(d.payment_id)}</strong></div></div>${rawDetails('查看 API 详情',d)}</div>`;await loadFinance();await openPaymentDetail(payment.payment_id,'finance')}catch(e){target.innerHTML=errorPanel(e,'退款失败')}}

function renderRefundResult(d,payment){const success=d.refund_status==='SUCCEEDED';const failed=d.refund_status==='FAILED';const processing=d.refund_status==='PROCESSING';const title=success?'退款已成功':failed?'退款失败：Provider 未受理':processing?'退款请求处理中':'退款结果：'+statusText(d.refund_status);const next=success?'Credits 已通过 REFUND_REVERSAL Ledger 释放':failed?'Credit Hold 已释放；请查看 Provider 调用和 Trace ID':'结果未知，先主动查退款，不要重复创建退款';return `<div class="result ${success?'success':failed?'error':'warning'}"><div class="result-title">${title}</div><div class="result-grid"><div class="result-item"><span>退款 ID（Refund ID）</span><strong>${copyButton(d.refund_id)}</strong></div><div class="result-item"><span>金额</span><strong>${money(payment.amount,payment.currency)}</strong></div><div class="result-item"><span>当前状态</span><strong class="status ${statusClass(d.refund_status)}">${esc(statusText(d.refund_status))}</strong></div><div class="result-item"><span>支付订单 ID</span><strong>${copyButton(d.payment_id||payment.payment_id)}</strong></div><div class="result-item"><span>下一步</span><strong>${next}</strong></div></div><div class="action-row">${processing?`<button class="secondary" onclick="queryRefund('${esc(d.refund_id)}','${esc(payment.payment_id)}',this)">主动查退款</button>`:''}<button class="secondary" onclick="openPaymentDetail('${esc(payment.payment_id)}','finance')">查看支付详情</button></div>${rawDetails('查看 API 详情',d)}</div>`}
async function executeRefund(payment){const target=document.getElementById('finance-feedback');target.innerHTML='<div class="result warning">处理中…正在创建退款订单（RefundOrder）。</div>';try{const d=await api('/refunds',{method:'POST',headers:{...auth('finance'),'Content-Type':'application/json','Idempotency-Key':'ui-refund-'+payment.payment_id},body:JSON.stringify({payment_id:payment.payment_id})});target.innerHTML=renderRefundResult(d,payment);await loadFinance();await openPaymentDetail(payment.payment_id,'finance')}catch(e){target.innerHTML=errorPanel(e,'退款失败')}}
async function queryRefund(refundId,paymentId,button){await runAction(button,'查退款中…',async()=>{try{const d=await api('/refunds/'+encodeURIComponent(refundId)+'/query',{method:'POST',headers:auth('finance')});const payment=await api('/payments/'+encodeURIComponent(paymentId),{headers:auth('finance')});document.getElementById('finance-feedback').innerHTML=renderRefundResult(d,payment);await loadFinance()}catch(e){document.getElementById('finance-feedback').innerHTML=errorPanel(e,'主动查退款失败')}})}

async function fde(){setApp(roleGuide('FDE / Admin','负责客户接入、联调和问题排查的工程师','快速判断问题发生在 API、Provider、Webhook、状态机还是 Ledger','拿到一个 Payment ID / Application ID / Trace ID → 查询 → 沿调用链定位', [['Trace ID','一次请求链路的关联 ID'],['Provider Call','服务端调用支付 Provider 的记录'],['Reconciliation','主动查单，用于恢复未知或滞后的状态'],['Audit','关键操作和权限决策的审计记录']])+`<section class="hero compact"><div class="eyebrow">排障场景 · FDE / Admin</div><h2>用一个 ID 串起完整调用链</h2><p>输入一个关联 ID，按 <strong>API 请求 → Provider 调用 → Webhook → 状态变化 → Ledger / Audit</strong> 查看安全调试信息。</p></section><div class="grid"><section class="card"><h3>开始一次排查</h3><div class="row"><div><label for="debug-payment">Payment ID（支付订单）</label><input id="debug-payment" placeholder="例如 payment_xxx"></div><div><label for="debug-application">Application ID（VCC 申请）</label><input id="debug-application" placeholder="例如 vcc_xxx"></div><div><label for="debug-trace">Trace ID（完整链路 ID）</label><input id="debug-trace" placeholder="粘贴完整 Trace ID"></div><button onclick="findFde(this)">查询调用链</button></div><div class="muted small" style="margin-top:8px">至少填写一个字段。建议优先使用客户提供的 Payment ID 或 Trace ID。</div><div id="debug-result"></div></section><section class="card half"><h3>你会看到什么？</h3><p class="muted small">这里展示可安全关联的请求/响应元数据、HTTP 结果、错误、Audit 和耗时。原始 Provider Body、签名 Header 和敏感字段遵循安全规则，不持久化。</p><div class="pill-row"><span class="pill">完整 Trace ID</span><span class="pill">Provider Request ID</span><span class="pill">错误与耗时</span></div></section></div>`)}
async function findFde(button){const payment=document.getElementById('debug-payment').value.trim();const application=document.getElementById('debug-application').value.trim();const trace=document.getElementById('debug-trace').value.trim();if(!payment&&!application&&!trace){document.getElementById('debug-result').innerHTML='<div class="error-panel"><div class="error-title">需要一个查询条件</div><div>请输入 Payment ID、Application ID 或 Trace ID。</div></div>';return}await runAction(button,'查询中…',async()=>{try{const qs=new URLSearchParams();if(payment)qs.set('payment_id',payment);if(application)qs.set('application_id',application);if(trace)qs.set('trace_id',trace);const d=await api('/admin/debug?'+qs.toString(),{headers:auth('admin')});document.getElementById('debug-result').innerHTML=renderDebug(d);if(d.payment?.payment_id){const timeline=await api('/payments/'+encodeURIComponent(d.payment.payment_id)+'/timeline',{headers:auth('admin')});document.getElementById('debug-result').insertAdjacentHTML('beforeend',`<div class="debug-block"><h3>支付状态时间线（Payment Timeline）</h3>${renderTimeline(timeline.events)}</div>`)} }catch(e){document.getElementById('debug-result').innerHTML=errorPanel(e,'FDE 查询失败')}})}
function renderDebug(d){const nodes=[];if(d.audit?.length)nodes.push(['API 请求','审计 '+d.audit[0].action]);(d.provider_calls||[]).forEach(c=>nodes.push(['Provider 调用',c.operation]));(d.webhooks||[]).forEach(w=>nodes.push(['Webhook 通知',w.provider_status||w.status]));(d.audit||[]).filter(a=>a.action==='PAYMENT_OBSERVATION'||a.action==='RECONCILE_PAYMENT'||a.action==='CREATE_VCC_APPLICATION'||a.action==='APPROVE_VCC'||a.action==='CREATE_VCC').forEach(a=>nodes.push(['状态 / 审计',a.outcome]));if(d.payment)nodes.push(['Ledger / 支付订单',d.payment.payment_status]);const chain=nodes.length?`<div class="debug-chain">${nodes.map(n=>`<div class="chain-node"><strong>${esc(n[0])}</strong><span>${esc(n[1])}</span></div>`).join('')}</div>`:'<div class="empty">没有匹配的调用记录</div>';const calls=(d.provider_calls||[]).map(c=>`<div class="debug-entry"><div class="debug-entry-head"><strong>${esc(c.operation)}</strong><span class="status ${c.success?'good':'bad'}">${c.success?'成功':'失败'}</span></div><div class="debug-grid"><div><span>Trace ID</span><strong>${copyButton(c.trace_id)}</strong></div><div><span>Provider 请求 ID</span><strong>${copyButton(c.provider_request_id)}</strong></div><div><span>HTTP / 耗时</span><strong>${esc(c.http_status||'—')} / ${esc(c.latency_ms??'—')} ms</strong></div><div><span>请求</span><strong>${esc(c.operation)}</strong></div><div><span>响应</span><strong>${esc(c.provider_code||'HTTP '+(c.http_status||'—'))}</strong></div><div><span>错误</span><strong>${esc(c.error_type||'—')}</strong></div></div>${rawDetails('查看安全调试详情',{request:{operation:c.operation,provider_request_id:c.provider_request_id},response:{http_status:c.http_status,provider_code:c.provider_code,success:c.success,error_type:c.error_type},headers:'原始签名 Header 未持久化',latency_ms:c.latency_ms})}</div>`).join('');const hooks=(d.webhooks||[]).map(w=>`<div class="debug-entry"><div class="debug-entry-head"><strong>${esc(w.event_type)} · ${esc(w.provider_status||'—')}</strong><span class="status ${statusClass(w.status)}">${esc(statusText(w.status))}</span></div><div class="debug-grid"><div><span>资源 ID</span><strong>${copyButton(w.resource_id)}</strong></div><div><span>接收时间</span><strong>${fmtTime(w.received_at)}</strong></div><div><span>处理时间</span><strong>${fmtTime(w.processed_at)}</strong></div><div><span>错误</span><strong>${esc(w.error||'—')}</strong></div></div></div>`).join('');const audits=(d.audit||[]).map(a=>`<div class="debug-entry"><div class="debug-entry-head"><strong>${esc(a.action)} · ${esc(a.outcome)}</strong><span>${fmtTime(a.created_at)}</span></div><div class="debug-grid"><div><span>资源</span><strong>${esc(a.resource_type)} / ${esc(a.resource_id||'—')}</strong></div><div><span>操作者</span><strong>${esc(a.actor_role||'system')}</strong></div><div><span>Trace ID</span><strong>${copyButton(a.trace_id)}</strong></div></div></div>`).join('');return `${d.payment?`<div class="result"><div class="result-title">支付订单（Payment）${esc(d.payment.payment_id)}</div><div class="result-grid"><div class="result-item"><span>金额</span><strong>${money(d.payment.amount,d.payment.currency)}</strong></div><div class="result-item"><span>本地状态</span><strong class="status ${statusClass(d.payment.payment_status)}">${esc(statusText(d.payment.payment_status))}</strong></div><div class="result-item"><span>Provider 状态</span><strong>${esc(d.payment.provider_status||'—')}</strong></div><div class="result-item"><span>支付订单 ID</span><strong>${copyButton(d.payment.payment_id)}</strong></div></div></div>`:''}${d.application?`<div class="result"><div class="result-title">VCC 申请（Application）${esc(d.application.application_id)}</div><div class="result-grid"><div class="result-item"><span>供应商</span><strong>${esc(d.application.vendor)}</strong></div><div class="result-item"><span>金额</span><strong>${money(d.application.amount,d.application.currency)}</strong></div><div class="result-item"><span>本地状态</span><strong>${esc(statusText(d.application.status))}</strong></div><div class="result-item"><span>审批</span><strong>${esc(statusText(d.application.approval_status))}</strong></div></div></div>`:''}${chain}<div class="debug-block"><h3>Provider 调用记录</h3>${calls||'<div class="empty">暂无 Provider 调用</div>'}</div><div class="debug-block"><h3>Webhook 通知</h3>${hooks||'<div class="empty">暂无 Webhook</div>'}</div><div class="debug-block"><h3>审计与状态变化</h3>${audits||'<div class="empty">暂无审计记录</div>'}</div><div class="muted small" style="margin-top:13px">${esc(d.retention_note||'')}</div>`}

const VCC_STEPS=['自然语言申请','参数解析','预算检查','角色权限','人工审批','人工确认','创建虚拟卡','完成'];
function vcc(){setApp(roleGuide('财务申请人（Finance）','需要为 AWS、SaaS 等企业支出申请虚拟卡的人员','把自然语言申请变成受预算、RBAC 和人工审批保护的开卡流程','描述申请 → 补齐信息 → 看预算和审批结果 → 人工确认后创建 Mock VCC', [['VCC','Virtual Card，企业虚拟卡'],['Budget Check','检查本月预算是否足够'],['RBAC','角色权限；申请人和审批人分离'],['Human Confirmation','创建卡前的最后一次人工确认']])+`<section class="hero compact"><div class="eyebrow">企业支出场景 · VCC Workflow Assistant</div><h2>按安全步骤申请一张企业虚拟卡</h2><p>你可以用自然语言描述支出申请；系统会依次完成参数解析、预算检查、角色权限、审批和人工确认，最后创建 Mock VCC。</p></section><section class="card"><div id="vcc-steps" class="steps"></div><div class="grid"><div class="half"><h3>1. 描述你的用卡申请</h3><div id="vcc-conversation" class="conversation"></div><label id="vcc-message-label" for="vcc-message">申请内容</label><textarea id="vcc-message">为 AWS 9 月账单申请一张 20000 USD 的虚拟卡，有效期 30 天</textarea><div class="muted small">可以分多轮补充。建议包含：供应商、金额和币种、用途、有效期。</div><button id="vcc-parse" onclick="parseVcc(this)">解析申请并检查预算</button><div id="vcc-error"></div></div><div class="half"><h3>2. 审批并创建虚拟卡</h3><div id="vcc-context" class="result"><div class="muted">完成解析后，这里会显示已识别信息、预算、角色权限和审批前置条件。</div></div><div class="action-row"><button class="good" id="approve-vcc" disabled onclick="approveVcc(this)">审批人确认</button><button id="create-vcc" disabled onclick="createCard(this)">确认创建虚拟卡</button></div><div id="vcc-result"></div></div></div></section>`);updateVccSteps(0);const query=new URLSearchParams(location.search);if(query.get('demo')==='vcc')document.getElementById('vcc-message')?.focus()}
function updateVccSteps(current,blocked=false){document.getElementById('vcc-steps').innerHTML=VCC_STEPS.map((s,i)=>`<div class="step ${i<current?'done':i===current?'current':''} ${blocked&&i>current?'blocked':''}"><span class="step-index">${i<current?'✓':i+1}</span><span>${s}</span></div>`).join('')}
function renderVccConversation(){const target=document.getElementById('vcc-conversation');if(!target)return;target.innerHTML=vccConversation.map(item=>`<div class="conversation-item ${item.role}"><strong>${item.role==='user'?'你':'助手'}</strong>：${esc(item.text)}</div>`).join('')}
function appendVccAssistant(text){if(text){vccConversation.push({role:'assistant',text});renderVccConversation()}}
function vccMissingLabels(fields){return (fields||[]).map(field=>({vendor:'供应商',amount:'金额',currency:'币种',request:'完整的用卡申请'}[field]||field))}
function renderVccContext(d){const p=d.parsed||{};const budget=d.budget;const approval=d.approval;const vendor=p.vendor&&p.vendor!=='UNKNOWN'?p.vendor:'未识别';const period=p.period_days?`${p.period_days} 天${currentVcc.periodExplicit?'':'（默认值，建议确认）'}`:'待补充';if(d.status==='OUT_OF_SCOPE')return `<div class="result-title">这个请求不在企业 VCC 申请范围内</div><p>${esc(d.scope_message||'当前只处理企业虚拟卡申请。')}</p><ul class="scope-list"><li>解析企业用卡申请</li><li>执行预算检查和角色权限（RBAC）</li><li>完成审批后创建 Mock 虚拟卡</li></ul><div class="clarify-box"><strong>可以这样说</strong>为 AWS 申请一张 20,000 USD 的虚拟卡，用于 9 月账单，有效期 30 天。</div>${rawDetails('查看 API 详情',d)}`;if(d.status==='REJECTED_UNSAFE_REQUEST')return `<div class="result-title">这个请求不能执行</div><p>我不能绕过审批、预算或系统安全规则，也不会处理完整卡号、CVV 等敏感信息。</p><div class="clarify-box"><strong>我可以帮助你</strong>提交企业 VCC 申请、检查预算、执行人工审批并创建 Mock 虚拟卡。</div>${rawDetails('查看 API 详情',d)}`;const missing=vccMissingLabels(p.missing_fields);const needsClarification=d.status==='NEEDS_CLARIFICATION';const budgetText=budget?(budget.passed?'通过':'未通过'):needsClarification?'未执行':'—';const approvalText=approval?(approval.required?'需要审批人':'自动通过'):needsClarification?'未执行':'—';return `<div class="result-title">${needsClarification?'需要补充信息':d.status==='REJECTED_BUDGET'?'预算未通过':'申请已解析'}</div><div class="result-grid"><div class="result-item"><span>供应商（Vendor）</span><strong>${esc(vendor)}</strong></div><div class="result-item"><span>金额</span><strong>${money(p.amount,p.currency||'USD')}</strong></div><div class="result-item"><span>有效期</span><strong>${esc(period)}</strong></div><div class="result-item"><span>预算检查（Budget Check）</span><strong class="status ${budget?(budget.passed?'good':'bad'):''}">${esc(budgetText)}</strong></div><div class="result-item"><span>角色权限（RBAC）</span><strong>${needsClarification?'等待申请完整':'申请人：Finance；审批人：Approver（角色分离）'}</strong></div><div class="result-item"><span>审批（Approval）</span><strong>${esc(approvalText)}</strong></div><div class="result-item"><span>申请 ID（Application ID）</span><strong>${copyButton(d.application_id)||'尚未创建申请'}</strong></div></div>${needsClarification?`<div class="clarify-box"><strong>还需要补充：${esc(missing.join('、')||'完整申请')}</strong>${esc(d.clarification_question||'请补充上述信息后继续。')}</div>`:`<div class="muted small">${esc(approval?.reason||budget?.reason||'解析结果来自确定性解析器；高风险动作仍由后端重新校验。')}</div>`}${rawDetails('查看 API 详情',d)}`}
async function parseVcc(button){const target=document.getElementById('vcc-context');const error=document.getElementById('vcc-error');const input=document.getElementById('vcc-message');const message=input.value.trim();if(!message){error.innerHTML=errorPanel({message:'请补充申请内容'},'需要继续输入');return}error.innerHTML='';vccConversation.push({role:'user',text:message});renderVccConversation();const combined=vccConversation.filter(item=>item.role==='user').map(item=>item.text).join('\n');const periodExplicit=/(有效期|周期|period|for)\s*\d+\s*(天|day|days)/i.test(combined);target.className='result warning';target.innerHTML='<div class="muted">处理中…正在判断意图并解析参数。</div>';await runAction(button,'解析中…',async()=>{try{const d=await api('/vcc/agent',{method:'POST',headers:{...auth('finance'),'Content-Type':'application/json'},body:JSON.stringify({message:combined})});currentVcc={applicationId:d.application_id,parsed:d.parsed,data:d,periodExplicit};target.className='result '+(d.status==='REJECTED_BUDGET'||d.status==='REJECTED_UNSAFE_REQUEST'?'error':d.status==='OUT_OF_SCOPE'||d.status==='NEEDS_CLARIFICATION'?'warning':'success');target.innerHTML=renderVccContext(d);const needsApproval=d.approval?.required&&d.application_id;document.getElementById('approve-vcc').disabled=!needsApproval;document.getElementById('create-vcc').disabled=!d.application_id||Boolean(needsApproval);const blocked=d.status==='NEEDS_CLARIFICATION'||d.status==='OUT_OF_SCOPE'||d.status==='REJECTED_UNSAFE_REQUEST';updateVccSteps(blocked?d.status==='NEEDS_CLARIFICATION'?1:0:d.budget?.passed===false?2:needsApproval?4:6,blocked);if(d.status==='NEEDS_CLARIFICATION'){appendVccAssistant(d.clarification_question||'请补充缺少的信息。');input.value='';input.placeholder='例如：20,000 USD，用于 9 月账单，有效期 30 天';document.getElementById('vcc-message-label').textContent='补充信息';document.getElementById('vcc-parse').textContent='继续解析';document.getElementById('vcc-result').innerHTML='<div class="result warning"><strong>后续步骤已锁定</strong><div class="muted small">补充关键信息后，系统会重新解析完整申请。</div></div>'}else if(d.status==='OUT_OF_SCOPE'){appendVccAssistant(d.scope_message||'请改为描述企业虚拟卡申请。');input.value='';input.placeholder='例如：为 AWS 申请 20,000 USD 虚拟卡';document.getElementById('vcc-message-label').textContent='重新描述申请';document.getElementById('vcc-parse').textContent='重新解析';document.getElementById('vcc-result').innerHTML=''}else if(d.status==='REJECTED_UNSAFE_REQUEST'){appendVccAssistant('这个请求不能执行。我可以帮助你按正常预算和审批流程申请 VCC。');document.getElementById('vcc-result').innerHTML=''}else{appendVccAssistant(d.application_id?'申请已完整解析，请继续查看 Budget、审批和人工确认步骤。':'申请已提交。')}}catch(e){error.innerHTML=errorPanel(e,'VCC 申请解析失败');updateVccSteps(0,true)}})}
async function approveVcc(button){const id=currentVcc.applicationId;if(!id){document.getElementById('vcc-error').innerHTML=errorPanel({message:'还没有可审批的申请 ID'},'审批前置条件未满足');return}openConfirm({sourceButton:button,title:'确认人工批准 VCC 申请？',description:'你将以 Approver（审批人）身份授权这笔企业支出。批准后，Finance 才能继续创建虚拟卡。',rows:[['申请 ID（Application ID）',id],['申请金额',money(currentVcc.parsed?.amount,currentVcc.parsed?.currency||'USD')],['当前状态','待审批（PENDING_APPROVAL）'],['操作影响','允许后续创建虚拟卡']],confirmText:'确认批准',onConfirm:async()=>{const target=document.getElementById('vcc-result');target.innerHTML='<div class="result warning">处理中…正在记录审批。</div>';try{const d=await api('/vcc/'+encodeURIComponent(id)+'/approve',{method:'POST',headers:{...auth('approver'),'Content-Type':'application/json'},body:JSON.stringify({approved:true})});button.dataset.keepDisabled='true';button.dataset.doneText='已审批';button.disabled=true;document.getElementById('create-vcc').disabled=false;updateVccSteps(6);target.innerHTML=`<div class="result success"><div class="result-title">人工审批已通过</div><div class="result-grid"><div class="result-item"><span>申请 ID（Application ID）</span><strong>${copyButton(d.application_id)}</strong></div><div class="result-item"><span>审批结果</span><strong>已批准（APPROVED）</strong></div><div class="result-item"><span>下一步</span><strong>Finance 可确认创建虚拟卡</strong></div></div>${rawDetails('查看 API 详情',d)}</div>`}catch(e){target.innerHTML=errorPanel(e,'VCC 审批失败')}}})}
async function createCard(button){const id=currentVcc.applicationId;if(!id){document.getElementById('vcc-error').innerHTML=errorPanel({message:'请先完成参数解析和审批'},'创建虚拟卡前置条件未满足');return}openConfirm({sourceButton:button,title:'确认创建企业虚拟卡？',description:'创建后会产生一张 Mock 虚拟卡。请确认申请已通过预算检查、角色权限与必要审批。',rows:[['申请 ID（Application ID）',id],['申请金额',money(currentVcc.parsed?.amount,currentVcc.parsed?.currency||'USD')],['操作影响','创建 Provider Mock Card，并进入已激活（ACTIVE）状态']],confirmText:'确认创建',onConfirm:async()=>{const target=document.getElementById('vcc-result');target.innerHTML='<div class="result warning">处理中…正在创建虚拟卡。</div>';try{const d=await api('/vcc/'+encodeURIComponent(id)+'/card',{method:'POST',headers:auth('finance')});button.dataset.keepDisabled='true';button.dataset.doneText='已创建';button.disabled=true;updateVccSteps(7);target.innerHTML=`<div class="result success"><div class="result-title">企业虚拟卡已创建</div><div class="result-grid"><div class="result-item"><span>状态</span><strong class="status good">已激活（ACTIVE）</strong></div><div class="result-item"><span>申请 ID（Application ID）</span><strong>${copyButton(d.application_id)}</strong></div><div class="result-item"><span>Provider Card ID</span><strong>${copyButton(d.provider_card_id)}</strong></div><div class="result-item"><span>脱敏卡号</span><strong>${esc(d.masked_card||'—')}</strong></div></div>${rawDetails('查看 API 详情',d)}</div>`}catch(e){target.innerHTML=errorPanel(e,'VCC 创建失败')}}})}

function webhookScenarioOptions(){return [['PENDING','PENDING：仍在处理中'],['SUCCESS','SUCCESS：支付成功'],['FAIL','FAIL：支付失败'],['CLOSE','CLOSE：支付关闭'],['AUTH_SUCCESS','AUTH_SUCCESS：转人工复核'],['UNKNOWN','UNKNOWN：非法 Provider 状态'],['UNKNOWN_PAYMENT','UNKNOWN_PAYMENT：订单不存在'],['AMOUNT_MISMATCH','AMOUNT_MISMATCH：金额不匹配'],['MISSING_AMOUNT','MISSING_AMOUNT：缺少金额']].map(x=>`<option value="${x[0]}">${x[1]}</option>`).join('')}
function renderPaymentResult(target,d,request,kind,details=lastApi){const status=d.payment_status||d.status;const success=status==='SUCCEEDED';const failure=['FAILED','CANCELLED'].includes(status);const title=success?'充值已成功':status==='PROCESSING'?'订单已创建，等待 Provider 确认':status==='REVIEW_REQUIRED'?'支付已转人工复核':failure?'充值未成功：'+statusText(status):'充值结果：'+esc(statusText(status));const next=success?'Credits 已通过 Ledger 入账（只入账一次）':status==='PROCESSING'?'选择一种 Webhook 结果，或打开详情执行主动查单':status==='REVIEW_REQUIRED'?'等待 Provider / FDE 完成人工复核；Credits 不入账':failure?'Provider 未成功，Credits 不入账；请查看 Timeline 和失败原因':'查看 API 详情并按建议恢复';const webhookControls=status==='PROCESSING'?`<div class="action-row"><div><label for="webhook-scenario">Webhook 测试结果</label><select id="webhook-scenario">${webhookScenarioOptions()}</select></div><button class="good" onclick="mockWebhook('${esc(d.payment_id)}','${esc(request.amount)}',this)">发送 Webhook</button><button class="secondary" onclick="mockWebhook('${esc(d.payment_id)}','${esc(request.amount)}',this,'INVALID_SIGNATURE',true)">验签失败测试</button></div>`:'';target.className='result '+(success?'success':failure?'error':'warning');target.innerHTML=`<div class="result-title">${title}</div><div class="result-grid"><div class="result-item"><span>金额</span><strong>${money(request.amount,request.currency)}</strong></div><div class="result-item"><span>支付订单 ID（Payment ID）</span><strong>${copyButton(d.payment_id)}</strong></div><div class="result-item"><span>当前状态</span><strong class="status ${statusClass(status)}">${esc(statusText(status))}</strong></div><div class="result-item"><span>下一步</span><strong>${next}</strong></div></div>${webhookControls}<div class="action-row"><button class="secondary" onclick="openPaymentDetail('${esc(d.payment_id)}','developer')">查看支付状态时间线</button>${status==='PROCESSING'?`<button class="secondary" onclick="queryPayment('${esc(d.payment_id)}',this,'developer')">主动查单</button>`:''}</div>${rawDetails('查看 API 详情',details)}`}
async function mockWebhook(paymentId,amount,button,forcedStatus,invalidSignature=false){const selected=forcedStatus||document.getElementById('webhook-scenario')?.value||'SUCCESS';const amountMismatch=selected==='AMOUNT_MISMATCH';const missingAmount=selected==='MISSING_AMOUNT';const unknownPayment=selected==='UNKNOWN_PAYMENT';const status=['UNKNOWN_PAYMENT','AMOUNT_MISMATCH','MISSING_AMOUNT'].includes(selected)?'SUCCESS':selected;await runAction(button,'处理中…',async()=>{const body=JSON.stringify({partner_transaction_id:unknownPayment?'txn_unknown_'+paymentId:'txn_'+paymentId,status,amount:missingAmount?null:amountMismatch?(Number(amount)+1).toFixed(2):amount,currency:'USD'});try{const signature=invalidSignature?'invalid':await hmac(body);const webhookResponse=await api('/webhooks/pingpong/checkout',{method:'POST',headers:{'Content-Type':'application/json','X-Mock-Signature':signature},body});const payment=await api('/payments/'+encodeURIComponent(paymentId),{headers:auth('developer')});const target=document.getElementById('topup-result');if(target)renderPaymentResult(target,payment,{amount,currency:'USD'},'webhook',webhookResponse);toast(invalidSignature?'Webhook 验签失败，订单状态保持不变':unknownPayment?'Webhook 关联不到订单，订单状态保持不变':amountMismatch?'Webhook 金额不匹配，订单状态保持不变':missingAmount?'Webhook 缺少金额，订单状态保持不变':'Webhook '+status+' 已处理');await refreshDeveloper()}catch(e){const target=document.getElementById('topup-result');if(target){if(invalidSignature){renderPaymentResult(target,{payment_id:paymentId,payment_status:'PROCESSING'},{amount,currency:'USD'},'webhook',lastApi);target.insertAdjacentHTML('beforeend',errorPanel(e,'Webhook 验签失败'))}else target.innerHTML=errorPanel(e,'Webhook 处理失败')}}})}
// 本轮客户演示层优化：保留既有 API handler，只覆盖页面编排，避免改变支付与审批业务逻辑。
function switchVccPersona(role, applicationId=''){
  const params=new URLSearchParams();
  params.set('persona',role==='approver'?'approver':'finance');
  const id=applicationId||localStorage.getItem('demo-vcc-application')||'';
  if(id)params.set('application_id',id);
  location.href='/ui/vcc?'+params.toString();
}
function vccPersonaTabs(role, applicationId=''){
  const id=applicationId||localStorage.getItem('demo-vcc-application')||'';
  return `<section class="persona-switch"><div><div class="persona-switch-label">当前角色：${role==='approver'?'Approver（审批人）':'Finance（申请人）'}</div><div class="muted small">Persona 切换只用于本地演示；每个操作仍由后端 Principal / RBAC 决定。</div></div><div class="persona-tabs"><button class="persona-tab ${role==='finance'?'active':''}" onclick="switchVccPersona('finance','${esc(id)}')">Finance 申请人</button><button class="persona-tab ${role==='approver'?'active':''}" onclick="switchVccPersona('approver','${esc(id)}')">Approver 审批人</button></div></section>`
}
function vccRoleChain(){return `<div class="role-separation"><div><strong>Requester · Finance</strong><span class="muted small">提交自然语言申请，不能执行审批。</span></div><div><strong>Approver · Approver</strong><span class="muted small">独立审核预算与用途，决定批准或拒绝。</span></div><div><strong>Final Human Confirmation</strong><span class="muted small">审批通过后由授权 Finance 最终确认开卡。</span></div></div>`}
const VCC_TEMPLATES={aws:{vendor:'AWS',amount:'20000',purpose:'9 月云服务账单',period:'30'},saas:{vendor:'SaaS Vendor',amount:'5000',purpose:'企业 SaaS 订阅费用',period:'30'},ads:{vendor:'Google Ads',amount:'10000',purpose:'广告投放费用',period:'30'},custom:{vendor:'',amount:'',purpose:'',period:'30'}};
let vccTemplateManual=false;
function vccTemplateMessage(){const vendor=document.getElementById('vcc-vendor')?.value.trim()||'供应商';const amount=document.getElementById('vcc-amount')?.value.trim()||'金额';const purpose=document.getElementById('vcc-purpose')?.value.trim()||'企业运营支出';const period=document.getElementById('vcc-period')?.value.trim()||'30';return `为 ${vendor} 申请一张 ${amount} USD 的虚拟卡，用于${purpose}，有效期 ${period} 天`}
function updateVccTemplatePreview(){const vendor=document.getElementById('vcc-vendor')?.value.trim();const amount=document.getElementById('vcc-amount')?.value.trim();const purpose=document.getElementById('vcc-purpose')?.value.trim();const period=document.getElementById('vcc-period')?.value.trim();const missing=[];if(!vendor)missing.push('供应商');if(!amount)missing.push('金额');if(!purpose)missing.push('用途');if(!period)missing.push('有效期');const message=vccTemplateMessage();const input=document.getElementById('vcc-message');if(input&&!vccTemplateManual)input.value=message;const preview=document.getElementById('vcc-preview');if(preview)preview.textContent=vccTemplateManual&&input?.value?input.value:message;const status=document.getElementById('vcc-builder-status');if(status)status.textContent=missing.length?'还需要填写：'+missing.join('、'):'已填写 4/4 个必填变量，提交前仍会由后端重新校验。';const button=document.getElementById('vcc-parse');if(button)button.disabled=Boolean(missing.length)&&!vccTemplateManual}
function applyVccTemplate(){const key=document.getElementById('vcc-template')?.value||'aws';const template=VCC_TEMPLATES[key]||VCC_TEMPLATES.aws;['vendor','amount','purpose','period'].forEach(field=>{const input=document.getElementById('vcc-'+field);if(input)input.value=template[field]});vccTemplateManual=false;updateVccTemplatePreview()}
function toggleVccManual(button){const input=document.getElementById('vcc-message');if(!input)return;const editing=input.hasAttribute('readonly');if(editing){input.removeAttribute('readonly');vccTemplateManual=true;button.textContent='恢复模板内容';const parse=document.getElementById('vcc-parse');if(parse)parse.disabled=false}else{input.setAttribute('readonly','');vccTemplateManual=false;button.textContent='编辑自由文本';updateVccTemplatePreview()}}
function vcc(){
  const params=new URLSearchParams(location.search);
  const role=params.get('persona')==='approver'?'approver':'finance';
  const applicationId=params.get('application_id')||localStorage.getItem('demo-vcc-application')||'';
  vccConversation=[];
  const guide=role==='approver'
    ? roleGuide('Approver（审批人）','负责独立审核企业 VCC 支出申请','确认申请人、金额、用途、Budget 和 Policy 结果后批准或拒绝','加载申请 → 核对信息 → 批准或拒绝 → 将决定返回申请人', [['Requester','提交申请的 Finance 用户'],['Approver','独立执行批准 / 拒绝的人'],['Policy','后端确定性规则，不由 AI 决定'],['Audit','记录审批动作和 Trace ID']])
    : roleGuide('Finance（申请人）','负责提交企业云资源和 SaaS 支出申请','把自然语言需求提交给 Budget、Policy、RBAC 和 Approver 流程','描述申请 → 查看等待审批状态 → 切换 Approver 视角 → 审批后回到 Finance 完成 Human Confirmation', [['Requester','提交申请，不负责批准自己'],['Budget Check','后端预算检查'],['RBAC','后端角色权限控制'],['Human Confirmation','批准后创建卡前的最后确认']]);
  const shell=role==='approver'?`<section class="hero compact"><div class="eyebrow">企业支出场景 · Approver Review</div><h2>审核待审批的企业 VCC 申请</h2><p>这是独立的审批人视角。申请由 Finance 提交；批准或拒绝会调用后端审批 API，并写入 Audit。</p></section>${vccPersonaTabs(role,applicationId)}${vccRoleChain()}<section class="card"><div class="section-head"><div><h3>待审批申请</h3><div class="muted small">输入 Finance 提交后生成的 Application ID，或直接使用本地演示中的最近申请。</div></div></div><div class="row"><div><label for="approver-application-id">Application ID（申请 ID）</label><input id="approver-application-id" placeholder="例如 vcc_xxx" value="${esc(applicationId)}"></div><button class="secondary" onclick="loadApproverApplication(this)">加载申请</button></div><div id="approver-context" class="result"><div class="persona-empty">加载申请后，这里会显示商户、金额、用途、Budget、Policy、申请人与当前状态。</div></div><div class="action-row"><button class="warn" id="reject-vcc" disabled onclick="rejectVcc(this)">拒绝</button><button class="good" id="approve-vcc" disabled onclick="approveVcc(this)">批准</button></div><div id="approver-result"></div></section>`:`<section class="hero compact"><div class="eyebrow">企业支出场景 · Finance Requester</div><h2>提交一笔受控的企业虚拟卡申请</h2><p>Finance 只负责提交申请。申请创建后会进入等待审批状态；请切换到 Approver 视角独立处理，批准后再回到 Finance 完成最终 Human Confirmation。</p></section>${vccPersonaTabs(role,applicationId)}${vccRoleChain()}<section class="card"><div id="vcc-steps" class="steps"></div><div class="grid"><div class="half"><h3>1. 创建结构化申请</h3><div id="vcc-conversation" class="conversation"></div><div class="vcc-builder"><div class="vcc-builder-title">推荐：选择模板并填写变量</div><p class="muted small">先选择常见支出场景，填写供应商、金额、用途和有效期。系统会自动生成标准申请文本，减少漏填和歧义。</p><label for="vcc-template">申请模板</label><select id="vcc-template" onchange="applyVccTemplate()"><option value="aws">AWS 云服务账单</option><option value="saas">SaaS 订阅费用</option><option value="ads">广告投放费用</option><option value="custom">自定义企业支出</option></select><div class="vcc-builder-grid"><div><label for="vcc-vendor">供应商</label><input id="vcc-vendor" value="AWS" oninput="updateVccTemplatePreview()" placeholder="例如 AWS"></div><div><label for="vcc-amount">金额（USD）</label><input id="vcc-amount" value="20000" inputmode="decimal" oninput="updateVccTemplatePreview()" placeholder="例如 20000"></div><div><label for="vcc-purpose">用途</label><input id="vcc-purpose" value="9 月云服务账单" oninput="updateVccTemplatePreview()" placeholder="例如 9 月云服务账单"></div><div><label for="vcc-period">有效期（天）</label><input id="vcc-period" value="30" inputmode="numeric" oninput="updateVccTemplatePreview()" placeholder="例如 30"></div></div><div class="muted small">币种：USD（当前 POC 支持范围）</div><div class="vcc-preview" id="vcc-preview">为 AWS 申请一张 20000 USD 的虚拟卡，用于 9 月云服务账单，有效期 30 天</div><div class="vcc-builder-status" id="vcc-builder-status">已填写 4/4 个必填变量，提交前仍会由后端重新校验。</div></div><details class="advanced-request"><summary>高级：自由编辑申请文本</summary><p class="muted small">模板适合正常流程；如需验证自然语言解析、补充信息或边界场景，可在这里编辑最终提交文本。</p><textarea id="vcc-message" readonly>为 AWS 申请一张 20000 USD 的虚拟卡，用于9 月云服务账单，有效期 30 天</textarea><button class="secondary" onclick="toggleVccManual(this)">编辑自由文本</button></details><button id="vcc-parse" onclick="parseVcc(this)">解析并提交申请</button><div id="vcc-error"></div></div><div class="half"><h3>2. 查看审批与最终确认</h3><div id="vcc-context" class="result"><div class="persona-empty">提交后，这里会显示申请已提交、当前状态、申请人和审批人。</div></div><div class="action-row"><button class="secondary" id="refresh-vcc" disabled onclick="loadFinanceApplication(this)">刷新审批状态</button><button id="create-vcc" disabled onclick="createCard(this)">确认创建虚拟卡</button></div><div id="vcc-result"></div></div></div></section>`;
  setApp(guide+shell);
  if(role==='finance'){
    updateVccSteps(0);
    applyVccTemplate();
    if(applicationId)loadFinanceApplication();
  }else if(applicationId){loadApproverApplication();}
}
function financeApplicationView(d){
  const parsed=d.parsed||{};
  const status=d.status||'PENDING_APPROVAL';
  const pending=status==='PENDING_APPROVAL';
  const approved=status==='APPROVED';
  const cardCreated=Boolean(d.provider_card_id);
  const title=pending?'申请已提交':approved?'审批已通过，可以进行最终确认':cardCreated?'虚拟卡已创建':status==='REJECTED'?'申请已拒绝':'申请状态：'+statusText(status);
  const statusLabel=pending?'等待审批':approved?'APPROVED（待 Finance 最终确认）':cardCreated?'ACTIVE':statusText(status);
  return `<div class="result-title">${title}</div><div class="result-grid"><div class="result-item"><span>商户（Vendor）</span><strong>${esc(d.vendor||parsed.vendor||'—')}</strong></div><div class="result-item"><span>金额</span><strong>${money(d.amount||parsed.amount,d.currency||parsed.currency||'USD')}</strong></div><div class="result-item"><span>用途</span><strong>${esc(d.purpose||parsed.purpose||'—')}</strong></div><div class="result-item"><span>当前状态</span><strong class="status ${statusClass(status)}">${esc(statusLabel)}</strong></div><div class="result-item"><span>申请人（Requester）</span><strong>Finance</strong></div><div class="result-item"><span>审批人（Approver）</span><strong>Approver</strong></div><div class="result-item"><span>Budget / Policy</span><strong class="status good">Passed（已由后端检查）</strong></div><div class="result-item"><span>Application ID</span><strong>${copyButton(d.application_id)}</strong></div></div>${pending?`<div class="clarify-box"><strong>下一步</strong>请切换到 Approver 视角，由独立审批人批准或拒绝；Finance 不能自行完成审批。<div class="action-row"><button class="secondary" onclick="switchVccPersona('approver','${esc(d.application_id)}')">切换到 Approver 视角</button></div></div>`:approved?'<div class="clarify-box"><strong>下一步</strong>Approval：APPROVED。请由 Finance / Authorized Operator 执行最终 Human Confirmation，再创建虚拟卡。</div>':''}${cardCreated?`<div class="clarify-box"><strong>已完成</strong>Provider Card ID：${copyButton(d.provider_card_id)}；脱敏卡号：${esc(d.masked_card||'—')}</div>`:''}${rawDetails('查看 API 详情',d)}`
}
function renderFinanceVcc(d){
  const p=d.parsed||{};
  const blocked=['NEEDS_CLARIFICATION','OUT_OF_SCOPE','REJECTED_UNSAFE_REQUEST','REJECTED_BUDGET'].includes(d.status);
  if(d.status==='NEEDS_CLARIFICATION')return `<div class="result-title">需要补充信息</div><div class="clarify-box"><strong>请补充：${esc(vccMissingLabels(p.missing_fields).join('、')||'完整申请')}</strong>${esc(d.clarification_question||'请补充后继续。')}</div>${rawDetails('查看 API 详情',d)}`;
  if(d.status==='REJECTED_BUDGET')return `<div class="result-title">预算未通过</div><p>后端 Budget Check 未通过，申请不会进入审批，也不会创建虚拟卡。</p>${rawDetails('查看 API 详情',d)}`;
  if(d.status==='OUT_OF_SCOPE'||d.status==='REJECTED_UNSAFE_REQUEST')return `<div class="result-title">申请未提交</div><p>${esc(d.scope_message||'当前请求不满足企业 VCC 流程要求。')}</p>${rawDetails('查看 API 详情',d)}`;
  return financeApplicationView(d);
}
async function parseVcc(button){
  const input=document.getElementById('vcc-message');
  const message=input?.value.trim();
  const target=document.getElementById('vcc-context');
  if(!message){document.getElementById('vcc-error').innerHTML=errorPanel({message:'请补充申请内容'},'申请内容为空');return}
  document.getElementById('vcc-error').innerHTML='';
  vccConversation.push({role:'user',text:message});renderVccConversation();
  const combined=vccConversation.filter(x=>x.role==='user').map(x=>x.text).join('\n');
  target.className='result warning';target.innerHTML='<div class="muted">处理中…正在解析并执行后端 Budget / Policy 检查。</div>';
  await runAction(button,'提交中…',async()=>{try{const d=await api('/vcc/agent',{method:'POST',headers:{...auth('finance'),'Content-Type':'application/json'},body:JSON.stringify({message:combined})});currentVcc={applicationId:d.application_id,parsed:d.parsed,data:d,periodExplicit:true};if(d.application_id)localStorage.setItem('demo-vcc-application',d.application_id);target.className='result '+(d.status==='PENDING_APPROVAL'||d.status==='APPROVED'?'success':'warning');target.innerHTML=renderFinanceVcc(d);document.getElementById('refresh-vcc').disabled=!d.application_id;document.getElementById('create-vcc').disabled=d.status!=='APPROVED';updateVccSteps(d.status==='PENDING_APPROVAL'?4:d.status==='APPROVED'?6:d.status==='NEEDS_CLARIFICATION'?1:0,blocked(d.status));if(d.status==='PENDING_APPROVAL')appendVccAssistant('申请已提交，当前等待独立 Approver 审批。');else if(d.status==='NEEDS_CLARIFICATION')appendVccAssistant(d.clarification_question||'请补充申请信息。');}catch(e){document.getElementById('vcc-error').innerHTML=errorPanel(e,'VCC 申请提交失败');updateVccSteps(0,true)}})
}
function blocked(status){return ['NEEDS_CLARIFICATION','OUT_OF_SCOPE','REJECTED_UNSAFE_REQUEST','REJECTED_BUDGET'].includes(status)}
function restartVcc(role='finance'){localStorage.removeItem('demo-vcc-application');currentVcc={applicationId:null,parsed:null,data:null,periodExplicit:false};location.href='/ui/vcc?persona='+(role==='approver'?'approver':'finance')}
function renderMissingVccApplication(target,role,id){localStorage.removeItem('demo-vcc-application');currentVcc={applicationId:null,parsed:null,data:null,periodExplicit:false};if(!target)return;target.className='result warning';target.innerHTML=`<div class="result-title">申请已不存在</div><p>Application ID <strong>${esc(id)}</strong> 在当前 Mock 数据库中找不到。</p><div class="clarify-box"><strong>这不代表审批失败</strong>本地 Mock 数据可能被重置，或浏览器保留了上一轮演示的旧 ID。请重新提交一笔申请，再继续审批流程。<div class="action-row"><button class="secondary" onclick="restartVcc('${role}')">重新开始申请</button></div></div>`;if(role==='finance'){const refresh=document.getElementById('refresh-vcc');if(refresh)refresh.disabled=true;const create=document.getElementById('create-vcc');if(create)create.disabled=true}else{const approve=document.getElementById('approve-vcc');if(approve)approve.disabled=true;const reject=document.getElementById('reject-vcc');if(reject)reject.disabled=true}}
async function loadFinanceApplication(button,explicitId=''){
  const id=explicitId||currentVcc.applicationId||localStorage.getItem('demo-vcc-application');
  if(!id){toast('还没有可刷新的 Application ID');return}
  currentVcc.applicationId=id;
  await runAction(button,'刷新中…',async()=>{try{const d=await api('/vcc/'+encodeURIComponent(id),{headers:auth('finance')});currentVcc.parsed={vendor:d.vendor,amount:d.amount,currency:d.currency,purpose:d.purpose,period_days:d.period_days};const target=document.getElementById('vcc-context');if(target)target.innerHTML=financeApplicationView(d);const create=document.getElementById('create-vcc');if(create)create.disabled=d.status!=='APPROVED'||Boolean(d.provider_card_id);const refresh=document.getElementById('refresh-vcc');if(refresh)refresh.disabled=false;updateVccSteps(d.status==='PENDING_APPROVAL'?4:d.status==='APPROVED'?6:d.provider_card_id?8:0);if(d.status==='APPROVED')toast('审批已通过：请完成最终 Human Confirmation')}catch(e){if(e.status===404){renderMissingVccApplication(document.getElementById('vcc-context'),'finance',id)}else{const target=document.getElementById('vcc-error');if(target)target.innerHTML=errorPanel(e,'审批状态刷新失败')}}})
}
function renderApproverVcc(d){
  const pending=d.status==='PENDING_APPROVAL';
  return `<div class="result-title">${pending?'待审批申请':'申请状态：'+esc(statusText(d.status))}</div><div class="result-grid"><div class="result-item"><span>商户（Vendor）</span><strong>${esc(d.vendor)}</strong></div><div class="result-item"><span>金额</span><strong>${money(d.amount,d.currency)}</strong></div><div class="result-item"><span>用途</span><strong>${esc(d.purpose)}</strong></div><div class="result-item"><span>有效期</span><strong>${esc(d.period_days)} 天</strong></div><div class="result-item"><span>Budget Check</span><strong class="status good">Passed</strong></div><div class="result-item"><span>Policy / RBAC</span><strong class="status good">Passed</strong></div><div class="result-item"><span>申请人（Requester）</span><strong>Finance${d.requester_id?' · '+esc(d.requester_id):''}</strong></div><div class="result-item"><span>当前审批状态</span><strong class="status ${statusClass(d.approval_status||d.status)}">${esc(statusText(d.approval_status||d.status))}</strong></div></div>${pending?'<div class="clarify-box"><strong>审批职责</strong>请核对业务用途与金额后选择批准或拒绝。该动作会通过 Approver Bearer token 调用后端 API。</div>':''}${rawDetails('查看 API 详情',d)}`
}
async function loadApproverApplication(button){
  const id=document.getElementById('approver-application-id')?.value.trim()||localStorage.getItem('demo-vcc-application');
  if(!id){document.getElementById('approver-result').innerHTML=errorPanel({message:'请输入 Application ID'},'无法加载申请');return}
  currentVcc.applicationId=id;localStorage.setItem('demo-vcc-application',id);
  await runAction(button,'加载中…',async()=>{try{const d=await api('/vcc/'+encodeURIComponent(id),{headers:auth('approver')});const target=document.getElementById('approver-context');target.className='result '+(d.status==='PENDING_APPROVAL'?'warning':'success');target.innerHTML=renderApproverVcc(d);const pending=d.status==='PENDING_APPROVAL';document.getElementById('approve-vcc').disabled=!pending;document.getElementById('reject-vcc').disabled=!pending}catch(e){if(e.status===404){renderMissingVccApplication(document.getElementById('approver-context'),'approver',id)}else document.getElementById('approver-context').innerHTML=errorPanel(e,'申请加载失败')}})
}
function decideVcc(button,approved){
  const id=currentVcc.applicationId;
  if(!id){document.getElementById('approver-result').innerHTML=errorPanel({message:'请先加载申请'},'审批前置条件未满足');return}
  openConfirm({sourceButton:button,title:approved?'确认批准 VCC 申请？':'确认拒绝 VCC 申请？',description:approved?'你将以 Approver 身份授权 Finance 后续进行最终 Human Confirmation。':'你将以 Approver 身份拒绝本次企业支出申请。',rows:[['Application ID',id],['审批决定',approved?'批准（APPROVED）':'拒绝（REJECTED）'],['执行角色','Approver'] ],danger:!approved,confirmText:approved?'确认批准':'确认拒绝',onConfirm:async()=>{try{const d=await api('/vcc/'+encodeURIComponent(id)+'/approve',{method:'POST',headers:{...auth('approver'),'Content-Type':'application/json'},body:JSON.stringify({approved})});document.getElementById('approver-result').innerHTML=`<div class="result success"><div class="result-title">审批动作已记录：${approved?'APPROVED':'REJECTED'}</div><p>${approved?'请切换回 Finance 视角完成最终 Human Confirmation。':'申请已结束，不会创建虚拟卡。'}</p>${rawDetails('查看 API 详情',d)}</div>`;await loadApproverApplication()}catch(e){document.getElementById('approver-result').innerHTML=errorPanel(e,'审批动作失败')}}})
}
function approveVcc(button){decideVcc(button,true)}
function rejectVcc(button){decideVcc(button,false)}
async function createCard(button){
  const id=currentVcc.applicationId;
  if(!id){document.getElementById('vcc-error').innerHTML=errorPanel({message:'请先提交并加载已批准的申请'},'开卡前置条件未满足');return}
  openConfirm({sourceButton:button,title:'确认创建企业虚拟卡？',description:'你将以 Finance / Authorized Operator 身份执行最终 Human Confirmation。',rows:[['Application ID',id],['操作角色','Finance / Authorized Operator'],['操作影响','调用后端开卡接口并创建 Mock Card']],confirmText:'确认创建',onConfirm:async()=>{try{const d=await api('/vcc/'+encodeURIComponent(id)+'/card',{method:'POST',headers:auth('finance')});button.dataset.keepDisabled='true';button.dataset.doneText='已创建';document.getElementById('vcc-result').innerHTML=`<div class="result success"><div class="result-title">企业虚拟卡已创建</div><div class="result-grid"><div class="result-item"><span>状态</span><strong class="status good">已激活（ACTIVE）</strong></div><div class="result-item"><span>Provider Card ID</span><strong>${copyButton(d.provider_card_id)}</strong></div><div class="result-item"><span>脱敏卡号</span><strong>${esc(d.masked_card||'—')}</strong></div></div>${rawDetails('查看 API 详情',d)}</div>`;await loadFinanceApplication()}catch(e){document.getElementById('vcc-result').innerHTML=errorPanel(e,'虚拟卡创建失败')}}})
}
async function developer(){setApp(roleGuide('开发者（Developer）','负责 AI 平台接入或充值体验的开发者','先完成一笔正常的 Credits 充值，确认 Payment 最终状态和余额入账','创建订单 → 点击成功 Webhook → 查看 Credits 与 Timeline', [['Credits','AI API 可消费的额度'],['Payment','充值订单，状态不会只由前端决定'],['Webhook','Provider 发来的支付状态通知'],['Reliability Lab','工程师专用的超时、429、失败与通知测试']])+`<section class="hero compact"><div class="eyebrow">客户场景 · AI Credits 收款</div><h2>开发者 Credits 充值</h2><p>先体验最正常的充值链路：创建 Checkout / Payment，等待 Provider 确认，再通过 Webhook 或主动查单完成 Credits 入账。</p><div class="business-flow"><span>创建 Checkout</span><i>→</i><span>Payment</span><i>→</i><span>Webhook / Query</span><i>→</i><span>Credits Ledger</span></div></section><div class="grid"><section class="card third"><div class="muted">可用 Credits</div><div class="metric" id="balance">—</div><div class="muted small">Credits 单位：USD_CREDIT</div></section><section class="card half"><h3>创建充值</h3><p class="muted small">默认走正常的 PENDING 流程。创建后，订单卡片会直接显示“模拟支付成功 Webhook”主操作。</p><label for="amount">充值金额（USD）</label><input id="amount" value="100.00" inputmode="decimal"><button class="good" onclick="topup(this)">创建充值订单</button><details class="reliability-lab"><summary>高级：Provider 创建阶段测试（工程师）</summary><p class="lab-note">默认正常流程不需要打开此处。这里只用于切换 Provider Create 的 PENDING、超时、限流、拒绝或人工复核场景。</p><label for="payment-scenario">Provider Create 场景</label><select id="payment-scenario"><option value="PENDING">Normal：PENDING，等待 Webhook</option><option value="SUCCESS">SUCCESS：创建即成功</option><option value="TIMEOUT">TIMEOUT：结果未知</option><option value="429">429：限流</option><option value="FAIL">Reject：Provider 拒绝</option><option value="CLOSE">CLOSE：Provider 关闭</option><option value="AUTH_SUCCESS">Manual Review：人工复核</option></select><p class="muted small">创建后如需测试异常通知，请在订单结果卡片中展开“异常 Webhook / 验签测试（工程师）”。</p></details><div id="topup-result" class="result"><div class="muted">创建后会显示 Payment 状态、成功 Webhook 操作和下一步动作。</div></div></section><section class="card"><div class="section-head"><div><h3>充值记录</h3><div class="muted small">处理中订单可主动查单；每笔记录都可以展开 Payment Timeline。</div></div><button class="secondary" onclick="refreshDeveloper(this)">刷新记录</button></div><div id="payments">加载中…</div><div id="payment-detail"></div></section></div>`);await refreshDeveloper();const query=new URLSearchParams(location.search);if(query.get('demo')==='credits')document.getElementById('amount')?.focus()}
function renderPaymentResult(target,d,request,kind,details=lastApi){
  const status=d.payment_status||d.status;const success=status==='SUCCEEDED';const failure=['FAILED','CANCELLED'].includes(status);
  const title=success?'充值已成功':status==='PROCESSING'?'订单已创建，等待 Provider 确认':status==='REVIEW_REQUIRED'?'支付已转人工复核':failure?'充值未成功：'+statusText(status):'充值结果：'+esc(statusText(status));
  const next=success?'Credits 已通过 Ledger 入账（只入账一次）':status==='PROCESSING'?'点击下方按钮模拟支付成功 Webhook，或使用主动查单恢复状态':status==='REVIEW_REQUIRED'?'等待 Provider / FDE 完成人工复核；Credits 不入账':failure?'Provider 未成功，Credits 不入账；请查看失败原因':'查看 API 详情并按建议恢复';
  const normalWebhook=status==='PROCESSING'?`<section class="webhook-next"><div class="webhook-next-title">下一步：模拟支付成功 Webhook</div><p>真实环境由 Provider 发起回调；当前 Mock 环境点击下方按钮即可模拟 SUCCESS 通知。</p><button class="good" onclick="mockWebhook('${esc(d.payment_id)}','${esc(request.amount)}',this,'SUCCESS')">模拟支付成功 Webhook</button></section>`:'';
  const lab=status==='PROCESSING'?`<details class="reliability-lab embedded-lab"><summary>高级：异常 Webhook / 验签测试（工程师）</summary><p class="lab-note">正常成功流程请使用上面的主操作。这里用于验证失败、重复、金额不匹配、未知订单、乱序和无效签名等边界情况。</p><div class="action-row"><div><label for="webhook-scenario">Webhook 测试结果</label><select id="webhook-scenario">${webhookScenarioOptions()}</select></div><button class="secondary" onclick="mockWebhook('${esc(d.payment_id)}','${esc(request.amount)}',this)">发送所选 Webhook</button><button class="secondary" onclick="mockWebhook('${esc(d.payment_id)}','${esc(request.amount)}',this,'INVALID_SIGNATURE',true)">验签失败测试</button></div></details>`:'';
  target.className='result '+(success?'success':failure?'error':'warning');target.innerHTML=`<div class="result-title">${title}</div><div class="result-grid"><div class="result-item"><span>金额</span><strong>${money(request.amount,request.currency)}</strong></div><div class="result-item"><span>支付订单 ID（Payment ID）</span><strong>${copyButton(d.payment_id)}</strong></div><div class="result-item"><span>当前状态</span><strong class="status ${statusClass(status)}">${esc(statusText(status))}</strong></div><div class="result-item"><span>下一步</span><strong>${next}</strong></div></div>${normalWebhook}<div class="action-row"><button class="secondary" onclick="openPaymentDetail('${esc(d.payment_id)}','developer')">查看支付状态时间线</button>${status==='PROCESSING'?`<button class="secondary" onclick="queryPayment('${esc(d.payment_id)}',this,'developer')">主动查单</button>`:''}</div>${lab}${rawDetails('查看 API 详情',details)}`
}

document.querySelectorAll('.nav').forEach(a=>{if(a.dataset.page===PAGE)a.classList.add('active')});loadEnvironment();({demo,developer,finance,fde,vcc}[PAGE]||demo)();
const showcasePayment=new URLSearchParams(location.search).get('payment_id');if(PAGE==='developer'&&showcasePayment)setTimeout(()=>openPaymentDetail(showcasePayment,'developer'),1800);
const showcaseApplication=new URLSearchParams(location.search).get('application_id');if(PAGE==='vcc'&&showcaseApplication&&new URLSearchParams(location.search).get('persona')!=='approver')setTimeout(()=>loadFinanceApplication(null,showcaseApplication),1200);
async function hmac(body){const key=await crypto.subtle.importKey('raw',new TextEncoder().encode('local-demo-secret'),{name:'HMAC',hash:'SHA-256'},false,['sign']);const sig=await crypto.subtle.sign('HMAC',key,new TextEncoder().encode(body));return [...new Uint8Array(sig)].map(x=>x.toString(16).padStart(2,'0')).join('')}
</script></body></html>'''


def page(name: str) -> HTMLResponse:
    return HTMLResponse(_HTML.replace("{{TITLE}}", _PAGE_TITLES[name]).replace("{{PAGE}}", name))


@router.get("/ui", include_in_schema=False)
def ui_home():
    return RedirectResponse("/ui/demo")


@router.get("/ui/{name}", include_in_schema=False)
def ui_page(name: str):
    if name not in _PAGE_TITLES:
        return RedirectResponse("/ui/demo")
    return page(name)
